services:
  postgres:
    container_name: chezcar_postgres
    image: postgres:17
    ports:
      - "5435:5432"
    volumes:
      - ./data/sales_inventory_postgres:/var/lib/postgresql/data
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: chezcar_db
