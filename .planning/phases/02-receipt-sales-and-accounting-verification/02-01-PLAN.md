---
phase: 02-receipt-sales-and-accounting-verification
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/20260827000000_sale_per_branch_receipt_verification/migration.sql
  - lib/server/services/customer-sales.ts
  - lib/server/services/notifications.ts
  - lib/server/policy/access.ts
  - lib/server/authorization.ts
  - app/api/sales/route.ts
  - "app/api/sales/[saleId]/review/route.ts"
  - "app/api/sales/[saleId]/route.ts"
  - lib/contracts/sales.ts
requirements: [REQ-sales-posting]
estimate:
  tokens: 58000
  raw_tokens: 29000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "Branch Staff can post one sale per unique branch+booklet+number receipt and the same number in a different branch succeeds"
    - "Duplicate receipt in same branch/booklet returns 409 DUPLICATE_RECEIPT"
    - "Posting deducts onHand atomically, never drives available stock negative, and creates immutable movements surviving reload"
    - "Accounting Staff can mark UNVERIFIED sale as VERIFIED and VERIFIED is terminal"
    - "BRANCH_STAFF cannot verify; ADMIN cannot verify (per ADR 0014 verification actor)"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Sale.receiptBooklet + composite @@unique([locationId, receiptBooklet, manualReceiptNumber]) + Sale.version + NotificationRelatedType.SALE + AccountingReviewStatus.MISMATCH_REPORTED"
    - path: "prisma/migrations/20260827000000_sale_per_branch_receipt_verification/migration.sql"
      provides: "Additive DB constraint enforcing per-branch receipt identity and terminal verification states"
    - path: "lib/server/services/customer-sales.ts"
      provides: "createDirectSale with per-branch P2002 handling, deductSaleLines guard, and reviewSale VERIFIED state machine"
    - path: "app/api/sales/route.ts"
      provides: "Branch-scoped POST /api/sales with Zod receiptBooklet and server-calculated totals"
    - path: "app/api/sales/[saleId]/review/route.ts"
      provides: "POST /api/sales/:id/review gated to ACCOUNTING_STAFF only for VERIFIED"
  key_links:
    - from: "app/api/sales/route.ts"
      to: "lib/server/services/customer-sales.ts:createDirectSale"
      via: "requireCapability + directSaleSchema (receiptBooklet) + actorLocationId scoping"
    - from: "lib/server/services/customer-sales.ts:reviewSale"
      to: "prisma.saleAccountingReview"
      via: "Serializable tx with SELECT FOR UPDATE and terminal VERIFIED guard"
    - from: "Sale composite unique"
      to: "ManualReceipt composite"
      via: "P2002 mapped to DUPLICATE_RECEIPT per locationId+booklet+number"
---

<objective>
Wire the thinnest end-to-end sales slice: Branch posts per-branch receipt sale → stock deduction → Accounting verifies VERIFIED. Fixes global receipt uniqueness (ADR 0014 §1) and establishes the verification state machine (ADR 0014 §3) without mismatch/correction.

Purpose: Prove the critical sale→stock→verify skeleton before expanding mismatch and void logic; unblock real booklet reuse that currently spuriously 409s.
Output: Additive migration, updated service/API with per-branch uniqueness, VERIFIED terminal transition, and a runnable happy-path proof.
</objective>

<execution_context>
@C:/Users/Jerum/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Jerum/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-receipt-sales-and-accounting-verification/RESEARCH.md
@docs/adr/0014-accounting-verification.md
@prisma/schema.prisma
@lib/server/services/customer-sales.ts
@lib/server/services/notifications.ts
@lib/server/policy/access.ts
@app/api/sales/route.ts
@app/api/sales/[saleId]/review/route.ts
@lib/server/services/stock-transfers.ts
</context>

<tasks>

<task type="tracer">
  <name>End-to-end per-branch receipt sale and Accounting VERIFIED</name>
  <files>prisma/schema.prisma, prisma/migrations/20260827000000_sale_per_branch_receipt_verification/migration.sql, lib/server/services/customer-sales.ts, lib/server/services/notifications.ts, app/api/sales/route.ts, app/api/sales/[saleId]/review/route.ts, app/api/sales/[saleId]/route.ts</files>
  <action>Implement the tracer slice end-to-end with one happy path only. Schema: add Sale.receiptBooklet String @default("") , Sale.version Int @default(1), add MismatchCategory enum placeholder is NOT required yet but rename AccountingReviewStatus.FLAGGED to MISMATCH_REPORTED via ALTER TYPE ... RENAME VALUE (or add MISMATCH_REPORTED and keep FLAGGED deprecated with check rejecting FLAGGED writes), add NotificationRelatedType.SALE via ALTER TYPE ... ADD VALUE (do not hold blocking tx), drop global Sale.manualReceiptNumber @unique and add @@unique([locationId, receiptBooklet, manualReceiptNumber]) plus same composite on ManualReceipt (add locationId String?, receiptBooklet String @default("") and @@unique([locationId, receiptBooklet, number])) with backfill "" for existing rows. Migration must be additive, hand-authored SQL, and include indexes. Service: update directSaleSchema to include receiptBooklet: z.string().trim().max(50).default("") and keep manualReceiptNumber validation, update releaseOrderSchema finalReceiptNumber to allow booklet prefix handling (reuse same field), update createDirectSale and releaseCustomerOrder to pass locationId+receiptBooklet+manualReceiptNumber to Prisma create and to catch P2002 on the new composite index mapping to CustomerSalesError DUPLICATE_RECEIPT 409 (check constraint name sales_location_receipt_unique). Keep server-calculated total refusing amountPaid !== total (INVALID_PAYMENT). Keep deductSaleLines guard onHand - reserved >= quantity and version optimistic increment; wrap in Prisma Serializable tx with lock pattern similar to stock-transfers.ts lockSale SELECT FOR UPDATE. Review: rewrite accountingReviewSchema to z.object({ status: z.enum(["VERIFIED"]) }) for tracer (mismatch deferred to 02-02), implement reviewSale to allow only ACCOUNTING_STAFF (assertAccountingStrict that rejects ADMIN per ADR 0014 §2; update assertAccounting to branch: ACCOUNTING_STAFF only), require current SaleAccountingReview.status === UNVERIFIED else throw INVALID_STATE 409, require Sale.status === POSTED else throw INVALID_STATE, use $transaction Serializable with SELECT FOR UPDATE on Sale and SaleAccountingReview, then update review to VERIFIED with reviewedById/At, forbid further transitions (terminal). API: keep GET /api/sales using saleScope branching, POST /api/sales still requireCapability customer-orders:view but service additionally enforces BranchOrAdmin branch scoping via actorLocationId; POST /api/sales/:saleId/review requires ACCOUNTING_STAFF capability check via requireCapability + assertAccountingStrict (return 403 FORBIDDEN for ADMIN and BRANCH/STOCK). Add GET /api/sales/:saleId returning serializeSale with receiptBooklet and reviewStatus for reload proof (reuse SALE_INCLUDE). Keep Decimal handling and serializeMoney. Do not implement mismatch, photo, correction, or order-linked restriction in this tracer.</action>
  <verify>
    <automated>npm run typecheck && npm run test:integration -- tests/integration/sales-tracer.test.ts 2>&1 | tail -40</automated>
  </verify>
  <done>Same booklet+number posted to BL and QC both succeed with separate Sales; duplicate to same branch returns 409; insufficient stock returns 409 and leaves onHand unchanged; Accounting marks UNVERIFIED → VERIFIED, reload shows VERIFIED, second verify 409; BRANCH/ADMIN verify attempts 403.</done>
</task>

<task type="auto">
  <name>Create tracer integration harness and factories</name>
  <files>tests/helpers/factories.ts, tests/helpers/database.ts, tests/integration/sales-tracer.test.ts</files>
  <action>Extend tests/helpers/factories to create per-role users (ADMIN, BRANCH_STAFF at BL and QC, ACCOUNTING_STAFF) with active location assignments and headers via authenticated session helper (follow existing user-management test helper pattern). Extend factories to seed products with price Decimal and InventoryBalance onHand/reserved/version for BL/QC. Create tests/integration/sales-tracer.test.ts that reuses disposable PostgreSQL 17 harness from Phase 1 (assertDisposableDatabaseUrl, no bind mount). Cover: per-branch reuse succeeds, duplicate same branch 409, insufficient stock 409, server total validation, stock deduction + CUSTOMER_ORDER_RELEASE/DIRECT_SALE movement created, sale survives reload via GET /api/sales/:id, Accounting VERIFIED succeeds, terminal guard and role guards, cross-branch isolation. Import service directly and via route fetch where applicable. Keep tests serial, use Serializable isolation expectations, and assert Prisma Decimal round-trip.</action>
  <verify>
    <automated>npm run test:integration -- tests/integration/sales-tracer.test.ts --run 2>&1 | grep -E "pass|fail|Tests"</automated>
  </verify>
  <done>Tracer harness runs against disposable PG and proves the single happy path end-to-end with no global-uniqueness regression.</done>
</task>

<task type="auto">
  <name>Add lib contract for receipt identity</name>
  <files>lib/contracts/sales.ts</files>
  <action>Create lib/contracts/sales.ts exporting Zod receiptBooklet helpers, SaleDto with receiptBooklet, branch, lines, reviewStatus, and SalesApiResponse types mirroring existing catalog/user contract style. Keep DTO branch-safe (no Prisma Decimal, use number). Document that receipt identity is locationId+receiptBooklet+manualReceiptNumber per ADR 0014 §1 and that manualReceiptNumber alone is not unique. Re-export mismatch enum placeholder for next plans. Do not import server-only prisma into DTO file.</action>
  <verify>
    <automated>npm run typecheck 2>&1 | tail -20</automated>
  </verify>
  <done>Contract file exists, typechecks, and documents per-branch identity for executors of later plans.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| client→API | Untrusted receipt identity, quantities, prices, actor spoof |
| API→service→DB | Authorization bypass, TOCTOU on receipt uniqueness and stock |
| worker→notification fan-out | Excessive/injected notifications |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-01-01 | Spoofing | POST /api/sales, POST /api/sales/:id/review | high | mitigate | requireCapability + validatePersistedAssignment + actorLocationId scoping; Accounting verify rejects ADMIN/BRANCH |
| T-02-01-02 | Tampering | receiptBooklet/manualReceiptNumber uniqueness | high | mitigate | Composite @@unique([locationId, receiptBooklet, manualReceiptNumber]) + P2002→409, not app check |
| T-02-01-03 | Tampering | stock deduction | critical | mitigate | deductSaleLines guard onHand - reserved >= qty + version optimistic lock + Serializable + FOR UPDATE |
| T-02-01-04 | Repudiation | sale posting | medium | mitigate | Sale.postedById/postedAt, SaleLine snapshot of itemCode/name/unitPrice, InventoryMovement actorId/reference |
| T-02-01-05 | DoS | inventory quantity | medium | mitigate | Zod positiveInt + money coerce, server-calculated total, INSUFFICIENT_STOCK 409 |
| T-02-01-SC | Tampering | prisma migration apply | high | mitigate | Hand-authored additive migration, no --reset, tested against disposable PG before deploy |
</threat_model>

<verification>
Tracer-only scope: per-branch receipt reuse, stock deduction, and VERIFIED terminality are proven by integration test against disposable PostgreSQL; negative paths return typed 409/403 envelopes.
</verification>

<success_criteria>
Branch Staff posts same booklet+number to two branches → 2 Sales POSTED with movements; duplicate to same branch → 409; insufficient stock → 409 with no negative; Accounting verifies UNVERIFIED → VERIFIED and reload preserves VERIFIED while second verify and non-Accounting verify are blocked.
</success_criteria>

<output>
Create .planning/phases/02-receipt-sales-and-accounting-verification/02-01-SUMMARY.md when done
</output>
