---
phase: 02-receipt-sales-and-accounting-verification
plan: 04
type: execute
wave: 4
depends_on: [02-03]
autonomous: true
files_modified:
  - app/sales/page.tsx
  - app/accounting/verify/page.tsx
  - app/accounting/mismatches/page.tsx
  - components/sales/sales-client.tsx
  - components/accounting/verify-client.tsx
  - lib/server/services/customer-sales.ts
  - docs/API.md
  - docs/DATABASE.md
  - docs/ARCHITECTURE.md
  - tests/integration/sales-operational.test.ts
requirements: [REQ-sales-posting, REQ-sales-reconciliation]
estimate:
  tokens: 56000
  raw_tokens: 28000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "Branch Staff sees own-branch sales and can post with server validation feedback surviving reload"
    - "Accounting sees UNVERIFIED queue with branch/booklet/number, lines, and verify vs mismatch actions"
    - "Admin sees mismatch inbox with correct/confirm actions and voided+replacement linking"
    - "Dashboard today/mtd sales, Accounting unverified/verified/mismatch counts, low-stock, and branch stock reflect VOIDED exclusion and correction net deltas without batch"
    - "All pages enforce server role/location scope; hidden buttons do not authorize"
  artifacts:
    - path: "app/sales/page.tsx"
      provides: "Server component for Branch sales post + list behind customer-orders:view"
    - path: "app/accounting/verify/page.tsx"
      provides: "Accounting UNVERIFIED queue with compare details"
    - path: "app/accounting/mismatches/page.tsx"
      provides: "Admin/Accounting mismatch resolution inbox"
    - path: "docs/API.md"
      provides: "Documented POST /api/sales, POST /api/sales/:id/review|correct|confirm contracts"
  key_links:
    - from: "app/sales/page.tsx → app/accounting/verify/page.tsx"
      to: "GET /api/sales scope"
      via: "Branch sees own location, Accounting sees all, VOIDED styled but queryable"
    - from: "lib/server/services/customer-sales.ts:getDashboardSummary/getReportsSummary"
      to: "Sale.status VOIDED exclusion"
      via: "Aggregates count only POSTED sales for totals; unverified/verified/mismatch counts use MISMATCH_REPORTED"
    - from: "client components"
      to: "server API"
      via: "same-origin fetch with credentials, Zod-parsed inputs, error envelope {error:{code,message}}"
---

<objective>
Harden operational surfaces: Branch sales UI, Accounting verify queue, Admin mismatch resolution inbox, dashboard/report totals reflecting durable sales, and phase coverage/docs.

Purpose: Make Phase 2 usable end-to-end for each role and update confidence docs without claiming batch processing.
Output: Three role-gated pages, dashboard/report fixes for VOIDED/correction, integration tests, and doc updates.
</objective>

<execution_context>
@C:/Users/Jerum/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Jerum/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-receipt-sales-and-accounting-verification/RESEARCH.md
@docs/adr/0014-accounting-verification.md
@lib/server/services/customer-sales.ts
@lib/server/policy/access.ts
@lib/menu.ts
@components/page-shell.tsx
@components/ui/card.tsx
@.planning/phases/01-trusted-foundation-and-data-onboarding/01-PATTERNS.md
@.planning/phases/01-trusted-foundation-and-data-onboarding/01-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Branch sales and Accounting verify/correct UI</name>
  <files>app/sales/page.tsx, app/accounting/verify/page.tsx, app/accounting/mismatches/page.tsx, components/sales/sales-client.tsx, components/accounting/verify-client.tsx, lib/menu.ts</files>
  <action>Build three server pages gated by requireCapability/requireUser per access map: app/sales/page.tsx (BRANCH_STAFF+ADMIN, branch-scoped list, post form with receiptBooklet+manualReceiptNumber+lines dynamic add, customer search optional, paymentMethod select, notes) delegates to components/sales/sales-client.tsx which is a focused client component using TanStack Query for GET /api/sales, applied filters (receiptBooklet, manualReceiptNumber, customer, status POSTED/VOIDED), pagination pageSize 10, placeholderData, and POST /api/sales mutation with Zod client validation and server error envelope display; show VOIDED badge and linked replacement reference when correctionOfId present. app/accounting/verify/page.tsx (ACCOUNTING_STAFF only) lists UNVERIFIED sales (server filters status UNVERIFIED) with full detail compare table (receiptBooklet/number, branch, postedBy, lines itemCode/name/qty/unitPrice/total, payment, notes) plus actions Mark VERIFIED (calls POST /api/sales/:id/review {status: VERIFIED}) and File Mismatch (dialog with mismatchCategory Select of 6 values, notes textarea required, receiptPhotoUrl input URL optional, submits {status: MISMATCH_REPORTED, ...}). app/accounting/mismatches/page.tsx (ADMIN or ACCOUNTING_STAFF) lists MISMATCH_REPORTED sales with mismatch badge, VOIDED originals, and actions Confirm Correct (POST /api/sales/:id/confirm) and Fix with Correction (dialog with new receipt identity + corrected lines table, calls POST /api/sales/:id/correct). All client components use PageShell, Card, Dialog, Input, Label, Select, Button, Badge from components/ui, Tailwind semantic tokens, cn(), Lucide icons, next/link, mobile-first overflow tables, and server-component-by-default delegation. Add menu entries filtered by lib/menu via policy capabilities (sales for BRANCH/ADMIN, accounting verify for ACCOUNTING, mismatches for ADMIN+ACCOUNTING) but do not rely on menu for auth. Preserve isFetching updating indicator and page reset on filter apply as in 01-PATTERNS app/products/page.tsx.</action>
  <verify>
    <automated>npm run typecheck && npm run build 2>&1 | tail -50</automated>
  </verify>
  <done>Three pages render server-gated; Branch can post and see own branch only; Accounting sees UNVERIFIED queue and can verify or file mismatch; Admin/Accounting sees mismatches and can correct/confirm; VOIDED styling and replacement links visible; unauthorized direct GET to page shows access-denied, not data.</done>
</task>

<task type="auto">
  <name>Dashboard and reports reflect durable sales without batch</name>
  <files>lib/server/services/customer-sales.ts</files>
  <action>Update getDashboardSummary to: filter todaySales/mtdSales aggregates where status POSTED only (exclude VOIDED), keep saleScope branching, keep flagged/unverified counts but replace FLAGGED with MISMATCH_REPORTED (count where status MISMATCH_REPORTED), ensure lowStock still filters available = onHand - reserved <= reorderLevel and respects branch scope for BRANCH_STAFF. Update getReportsSummary to: assertAccounting now allows ACCOUNTING_STAFF and ADMIN? Keep as is but ensure accounting flaggedRows use MISMATCH_REPORTED not FLAGGED, unverified/verified counts use new enum values, sales rows include receiptBooklet and voided/replacement fields, transactionCount counts POSTED only for totals but rows include VOIDED queryable. Add small helper to compute net sales after correction for dashboard: today's VOIDED reversal/correction movements already adjusted InventoryBalance so low-stock and branch stock reflect committed sale immediately; no batch job. Add serializeSale expansion to include receiptBooklet, version, voidedAt, voidedById, correctionOfId, replacementId (lookup via reverse relation), and receiptPhotoUrl/mismatch fields for reports. Keep all queries respecting saleScope/orderScope.</action>
  <verify>
    <automated>npm run test:integration -- tests/integration/sales-operational.test.ts --run -t "dashboard" 2>&1 | tail -30</automated>
  </verify>
  <done>Dashboard today/mtd and Accounting counts update immediately after posting correction, VOIDED excluded from totals but still listed, low-stock reflects net delta, branch vs accounting scopes correct.</done>
</task>

<task type="auto">
  <name>Operational integration tests and documentation</name>
  <files>tests/integration/sales-operational.test.ts, docs/API.md, docs/DATABASE.md, docs/ARCHITECTURE.md</files>
  <action>Tests: create tests/integration/sales-operational.test.ts covering cross-cutting criteria: (a) full lifecycle POST → VERIFY → MISMATCH → CORRECT → VOIDED+replacement auto-VERIFIED survives reload with movements, (b) duplicate receipt same branch blocked same tx and cross-branch allowed, (c) insufficient stock never negative including correction net-increase path, (d) role reach matrix: BRANCH can post/list own branch only and cannot verify/correct, ACCOUNTING can verify/mismatch but cannot post, ADMIN can correct/confirm but cannot verify, STOCK_STAFF denied all, (e) idempotent version conflict 409 and correctionOfId unique blocks second correction, (f) notification correctness already proven but re-assert mismatch/correction fan-out reaches ADMIN+postedBy, (g) order-linked correction blocked. Docs: update docs/API.md to document exact phase 2 routes POST /api/sales, GET /api/sales, GET /api/sales/:saleId, POST /api/sales/:saleId/review {VERIFIED|MISMATCH_REPORTED + mismatchCategory enum + notes + receiptPhotoUrl}, POST /api/sales/:saleId/correct, POST /api/sales/:saleId/confirm with status codes 200/400/403/404/409 envelopes and receiptBooklet field, note per-branch composite uniqueness error DUPLICATE_RECEIPT. Update docs/DATABASE.md with Sale.receiptBooklet, composite unique, MismatchCategory enum, SaleAccountingReview.receiptPhotoUrl, Sale.voided/correctionOfId/version, NotificationRelatedType.SALE, and note MANUAL adjustment to Sale_manualReceipt handling. Update docs/ARCHITECTURE.md route inventory and data flow for sales verification. Keep prototype vs durable distinction honest; do not claim daily batch or offline sale handling.</action>
  <verify>
    <automated>npm run typecheck && npm run build 2>&1 | tail -30</automated>
  </verify>
  <done>Operational tests pass on disposable PG, docs match implemented routes/schema/capabilities, build emits only known Recharts warnings, typecheck passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| client→API UI | Direct fetch bypassing button visibility |
| API→dashboard/reports | Leaked cross-branch data, stale totals |
| docs→producer | Overclaim of batch or offline behavior |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-04-01 | Elevation | sales/verify/mismatch pages | high | mitigate | Server requireCapability/requireUser per page + service actorLocationId scoping; menu is not ACL |
| T-02-04-02 | Information Disclosure | cross-branch sales list | medium | mitigate | listSales saleScope returns BRANCH_STAFF ? {locationId} : {} and tests assert isolation |
| T-02-04-03 | Tampering | dashboard totals include VOIDED | medium | mitigate | Aggregates where status POSTED only; verified/mismatch use MISMATCH_REPORTED |
| T-02-04-04 | Spoofing | correction via UI | medium | mitigate | Client POST goes through same gated API; server recomputes total and validates stock |
| T-02-04-05 | Denial of Service | large report fetch | low | accept | Take 200 limit on sales, 500 on inventory; Phase 5 adds pagination for reports |
| T-02-04-SC | Tampering | docs drift | low | mitigate | Docs updated only after implementation with verified routes en route |

</threat_model>

<verification>
Role-gated pages prove branch isolation and accounting verification without editing sales; dashboard/reports totals reflect durable sales immediately without batch job.
</verification>

<success_criteria>
Branch sales page posts and lists own branch; Accounting verify queue handles VERIFIED vs MISMATCH_REPORTED; mismatch inbox resolves via correct (VOIDED+replacement) or confirm; dashboard/mtd/accounting counts and low-stock update live and exclude VOIDED; docs list exact phase 2 contracts.
</success_criteria>

<output>
Create .planning/phases/02-receipt-sales-and-accounting-verification/02-04-SUMMARY.md when done
</output>
