---
phase: 02-receipt-sales-and-accounting-verification
plan: 03
type: execute
wave: 3
depends_on: [02-02]
autonomous: false
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/20260827000002_sale_void_replace/migration.sql
  - lib/server/services/customer-sales.ts
  - "app/api/sales/[saleId]/correct/route.ts"
  - "app/api/sales/[saleId]/confirm/route.ts"
  - lib/contracts/sales.ts
  - lib/server/policy/access.ts
requirements: [REQ-sales-posting, REQ-sales-reconciliation]
estimate:
  tokens: 64000
  raw_tokens: 32000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "MISMATCH_REPORTED sale can be corrected via void-and-replace that creates linked replacement sale auto-VERIFIED"
    - "Original sale becomes VOIDED with voidedAt/voidedById, corrected total/stock is server-calculated and validated against available stock"
    - "Confirm-correct keeps original, marks review VERIFIED with cleared mismatch fields and no stock change"
    - "VDDED and VERIFIED are terminal; second correction or verify from terminal is 409"
    - "Order-linked sales are rejected for correction with UNSUPPORTED_CORRECTION 409 in MVP"
    - "Concurrent correction races via version check return 409 STALE_VERSION and double-replacement is blocked by unique correctionOfId"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Sale.voidedAt, voidedById, correctionOfId FK self, version increment, related SaleCorrection relation"
    - path: "lib/server/services/customer-sales.ts"
      provides: "correctSale and confirmCorrectSale Serializable tx with compensating movements and state guards"
    - path: "app/api/sales/[saleId]/correct/route.ts"
      provides: "POST /api/sales/:id/correct gated to ADMIN or ACCOUNTING_STAFF"
    - path: "app/api/sales/[saleId]/confirm/route.ts"
      provides: "POST /api/sales/:id/confirm gated to ADMIN or ACCOUNTING_STAFF"
  key_links:
    - from: "app/api/sales/[saleId]/correct/route.ts"
      to: "lib/server/services/customer-sales.ts:correctSale"
      via: "ADMIN|ACCOUNTING check + version + corrected lines Zod"
    - from: "lib/server/services/customer-sales.ts:correctSale"
      to: "prisma.sale + inventoryBalance + inventoryMovement"
      via: "Serializable tx: lock Sale+Review+Balances, reversal movements, re-deduction, void original"
    - from: "Sale.correctionOfId @unique"
      to: "once-only replacement invariant"
      via: "DB unique prevents double replacement under race"
---

<objective>
Deliver auditable void-and-replace correction and confirm-correct per ADR 0014 §7 without ever mutating a posted sale in place, with compensating inventory movements in one transaction.

Purpose: Preserve audit history while allowing Admin/Accounting to fix encoding errors and to mark mistaken mismatch reports as VERIFIED.
Output: Linked replacement sales, voided originals, compensating movements, and gated correct/confirm APIs.
</objective>

<execution_context>
@C:/Users/Jerum/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Jerum/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-receipt-sales-and-accounting-verification/RESEARCH.md
@docs/adr/0014-accounting-verification.md
@prisma/schema.prisma
@lib/server/services/customer-sales.ts
@lib/server/services/stock-transfers.ts
@lib/server/services/notifications.ts
@lib/server/policy/access.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Schema correction linkage, void audit, and version</name>
  <files>prisma/schema.prisma, prisma/migrations/20260827000002_sale_void_replace/migration.sql, lib/contracts/sales.ts</files>
  <action>Schema additive migration 20260827000002: add to Sale voidedAt DateTime?, voidedById String? FK to User (relation SaleVoidedBy), correctionOfId String? @unique FK to Sale (relation SaleCorrection with fields [correctionOfId] references [id] onDelete Restrict), version already added in 02-01 but if missing add version Int @default(1), add @@index([correctionOfId]), keep voidedBy relation index. Do not add new InventoryMovementType values unless needed; reuse DIRECT_SALE with distinct reference/remarks as prescribed in RESEARCH Code Examples, but optionally add enum values SALE_VOID and SALE_CORRECTION via ALTER TYPE INVENTORY_MOVEMENT_TYPE ADD VALUE IF NOT EXISTS (idempotent). Migration must handle existing VOIDED rows (none yet) without backfill. Update lib/contracts/sales.ts to add CorrectSaleInput { receiptBooklet, manualReceiptNumber, paymentMethod, notes, lines: {productId, quantity, unitPrice}[], version }, ConfirmCorrectInput { notes, version }, and SaleDto voided fields. Mark this task reversible costly because FK shape locks audit queries.</action>
  <verify>
    <automated>npm run typecheck && npx -y prisma validate 2>&1 | tail -20</automated>
  </verify>
  <done>Prisma validates with new correction/void fields, unique correctionOfId, and optional movement type values.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Confirm one-way void-and-replace audit door before mutating stock</name>
  <decision>Confirm correction linkage and order-linked deferral before one-way void</decision>
  <context>Migration will add Sale.voidedAt/voidedById and correctionOfId @unique self-FK; next task will write VOIDED and compensating movements which cannot be undone without audit break. Confirm that order-linked sales will be blocked with 409 UNSUPPORTED_CORRECTION in MVP and that replacement via correctionOfId with auto-VERIFIED satisfies ADR 0014 7.</context>
  <options>
    <option id="approve">
      <name>Approve: block order-linked correction, linked replacement via correctionOfId, auto-VERIFIED</name>
      <pros>Clear MVP scope, audit preserved, matches ADR 0014 section 7, prevents orderId unique conflict</pros>
      <cons>Order-linked corrections deferred to later phase</cons>
    </option>
    <option id="revise">
      <name>Revise: allow order-linked correction with re-link to same CustomerOrder</name>
      <pros>Handles released-order corrections directly</pros>
      <cons>Requires dropping Sale.orderId unique and handling order status revert</cons>
    </option>
  </options>
  <resume-signal>Reply approve or revise with exact linking rule</resume-signal>
</task>

<task type="auto">
  <name>Void-and-replace and confirm-correct service plus gated routes</name>
  <files>lib/server/services/customer-sales.ts, app/api/sales/[saleId]/correct/route.ts, app/api/sales/[saleId]/confirm/route.ts, lib/server/services/notifications.ts, lib/server/policy/access.ts</files>
  <reversibility rating="one-way">Voiding marks original VOIDED and posts compensating movements that cannot be silently undone without audit break</reversibility>
  <action>Implement two service functions sharing correction authorization: function assertCorrectionActor(actor) allowing ADMIN or ACCOUNTING_STAFF only per ADR 0014 §7 (reject BRANCH_STAFF and STOCK_STAFF 403). Implement correctSale(actor, saleId, input) with Zod: receiptBooklet String max 50 default "", manualReceiptNumber String 1-100, paymentMethod enum, notes optional, lines array min 1 with productId, quantity positiveInt, unitPrice money optional, version Int. Flow inside prisma.$transaction Serializable: SELECT Sale+SaleAccountingReview+Location FOR UPDATE (prisma.$queryRaw SELECT ... FOR UPDATE), validate sale exists else 404, sale.orderId must be null else throw CustomerSalesError UNSUPPORTED_CORRECTION 409 "Order-linked sales cannot be corrected in MVP", validate sale.status === POSTED else 409 INVALID_STATE, validate review.status === MISMATCH_REPORTED else 409, validate sale.version === input.version else STALE_VERSION 409. Resolve products via activeProducts, compute server-authoritative total as sum quantity*(unitPrice ?? product.price), reject if no price. Validate stock for corrected quantities: for each product, compute net delta = correctedQty - originalQty (negative means less stock needed), if delta >0 then check InventoryBalance onHand - reserved >= delta else throw INSUFFICIENT_STOCK 409; never allow onHand &lt;0 or onHand &lt; reserved after net. Post compensating movements in same tx: for each original line create movement quantity +originalQty type DIRECT_SALE (or SALE_VOID) reference original.manualReceiptNumber remarks "Sale void reversal ${original.reference}"; for each corrected line create movement quantity -correctedQty type DIRECT_SALE (or SALE_CORRECTION) reference input.manualReceiptNumber remarks "Sale correction ${newReference} for ${original.reference}". Create replacement Sale with reference SALE-${randomUUID()}, same locationId, customerId original, receiptBooklet/input, manualReceiptNumber, paymentMethod, totalAmount, amountPaid = total, notes, postedById actor.userId, lines snapshot productItemCode/name/unitPrice Decimal, accountingReview create { status: VERIFIED } auto-VERIFIED per ADR, correctionOfId = original.id, version 1. Update original Sale status VOIDED, voidedAt now, voidedById actor, version increment. Deduct/adjust InventoryBalance: apply onHand decrements for positive net deltas already via movements loop but also execute inventoryBalance.updateMany with version check for each product net delta using updateMany pattern like stock-transfers decreaseAvailableBalance to prevent race. Create Sale.correction linking. Notify via notifyUsersForSale same-SET (ADMINs + original.postedById) with SUCCESS type title "Sale correction posted" description "${newSale.reference} corrects ${original.reference} at ${location.code}". Catch P2002 on composite receipt unique → DUPLICATE_RECEIPT 409 and on correctionOfId unique → ALREADY_CORRECTED 409. Implement confirmCorrectSale(actor, saleId, { notes, version }): same locks, require MISMATCH_REPORTED, set review to VERIFIED, clear mismatchCategory/notes/photoUrl, set notes to provided "Confirmed correct — ${notes}" if supplied, reviewedById/At, optional resolution = RESOLVED_MISTAKEN if schema has it, no stock change, notify with INFO "Mismatch confirmed correct" to same recipients. Routes: create app/api/sales/[saleId]/correct/route.ts and confirm/route.ts each with POST handler that calls requireCapability customer-orders:view? Actually correct/confirm need ADMIN|ACCOUNTING so use requireUser for those roles then delegate, or requireCapability with custom check; implement as requireUser(['ADMIN','ACCOUNTING_STAFF']) and return errorResponse mapping CustomerSalesError codes to status. Add GET hardening: listSales and GET /api/sales/:id must include correctionOfId, voidedAt/By, replacement reference expansion, and filter VOIDED from today's-sales dashboard aggregates but keep queryable. Keep idempotent retry: repeated correct with same version after VOIDED returns 409, not duplicate sale.</action>
  <verify>
    <automated>npm run typecheck && npm run test:integration -- tests/integration/sales-correction.test.ts --run 2>&1 | grep -E "pass|fail|Tests"</automated>
  </verify>
  <done>Fix-with-correction creates linked auto-VERIFIED replacement, voids original with movements reversing then re-deducting, validates non-negative stock, is idempotent via version+unique; confirm-correct transitions to VERIFIED without stock change; order-linked correction blocked; terminal guards enforced; notifications sent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| client→API correct/confirm | Forged correction with inflated totals or insufficient-stock bypass |
| service→DB | Double-replacement race, negative stock, orphaned order link |
| audit trail | VOIDED sale deletion or silent edit |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-03-01 | Tampering | correctSale stock net delta | critical | mitigate | onHand - reserved >= delta check inside Serializable + FOR UPDATE + version + updateMany count check |
| T-02-03-02 | Tampering | sale edit in place | high | mitigate | Never update SaleLine/amount on original; create new Sale row linked via correctionOfId, original immutable |
| T-02-03-03 | Repudiation | VOIDED without actor | medium | mitigate | Sale.voidedAt, voidedById, voidedBy relation, actor recorded on movements |
| T-02-03-04 | Tampering | double correction race | high | mitigate | correctionOfId @unique + version STALE_VERSION + FOR UPDATE on sale |
| T-02-03-05 | Elevation | BRANCH/STOCK correcting | high | mitigate | assertCorrectionActor ADMIN or ACCOUNTING_STAFF only, 403 for others |
| T-02-03-06 | Information Disclosure | order-linked correction orphan | medium | mitigate | Block when orderId != null with 409 UNSUPPORTED_CORRECTION, document via human-verify |
| T-02-03-SC | Tampering | migration void FK | high | mitigate | Hand-authored migration with FK restrict, tested on disposable PG, reversible only via new migration |

</threat_model>

<verification>
Correction and confirm-correct each run in one Serializable transaction with SELECT FOR UPDATE, version, and unique correctionOfId guards; compensating movements preserve no-negative-stock invariant and audit links.
</verification>

<success_criteria>
MISMATCH_REPORTED → correct creates VOIDED original + linked VERIFIED replacement with +originalQty and -correctedQty movements and SUCCESS notifications; MISMATCH_REPORTED → confirm creates VERIFIED with cleared mismatch and no movements; second correction or verify from terminal is 409; order-linked correction 409; BRANCH/STOCK correct 403; concurrent version conflict 409.
</success_criteria>

<output>
Create .planning/phases/02-receipt-sales-and-accounting-verification/02-03-SUMMARY.md when done
</output>
