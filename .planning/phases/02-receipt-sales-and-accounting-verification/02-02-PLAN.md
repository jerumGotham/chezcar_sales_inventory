---
phase: 02-receipt-sales-and-accounting-verification
plan: 02
type: execute
wave: 2
depends_on: [02-01]
autonomous: true
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/20260827000001_sale_mismatch_category/migration.sql
  - lib/server/services/customer-sales.ts
  - lib/server/services/notifications.ts
  - "app/api/sales/[saleId]/review/route.ts"
  - lib/contracts/sales.ts
requirements: [REQ-sales-reconciliation]
estimate:
  tokens: 52000
  raw_tokens: 26000
  tasks: 2
  confidence: med
must_haves:
  truths:
    - "Accounting can file MISMATCH_REPORTED with closed enum category, required notes, and optional receiptPhotoUrl"
    - "Free-text mismatch category is rejected with 400"
    - "VERIFIED requires no mismatch fields; MISMATCH_REPORTED requires category+notes"
    - "MISMATCH_REPORTED creates durable per-user WARNING notifications for all ADMIN + encoding BRANCH_STAFF"
    - "Terminal VERIFIED cannot transition to MISMATCH_REPORTED and vice versa without correction"
    - "Invalid category, missing notes, and empty photo string are rejected"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "MismatchCategory enum + SaleAccountingReview.mismatchCategory enum type + receiptPhotoUrl + resolution? field"
    - path: "lib/server/services/customer-sales.ts"
      provides: "accountingReviewSchema discriminated union + reviewSale state machine guards for MISMATCH_REPORTED"
    - path: "lib/server/services/notifications.ts"
      provides: "createNotifications with relatedType SALE handling"
  key_links:
    - from: "app/api/sales/[saleId]/review/route.ts"
      to: "lib/server/services/customer-sales.ts:reviewSale"
      via: "ACCOUNTING_STAFF-gated POST carrying mismatchCategory/notes/receiptPhotoUrl"
    - from: "lib/server/services/customer-sales.ts:reviewSale"
      to: "Notification rows"
      via: "same Serializable tx creates WARNING notifications for ADMIN ids + sale.postedById"
    - from: "SaleAccountingReview.mismatchCategory"
      to: "MismatchCategory enum"
      via: "Prisma enum + Zod z.enum validation rejecting free text"
---

<objective>
Add structured mismatch reporting per ADR 0014 §§4-6: closed MismatchCategory enum, required notes, optional photo URL, state-machine guards, and durable mismatch notifications.

Purpose: Make mismatches queryable for dashboard/reports and ensure branch/Admin learn of issues without losing history.
Output: Enum-constrained review service, extended schema with photoUrl, notification fan-out inside same transaction.
</objective>

<execution_context>
@C:/Users/Jerum/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Jerum/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-receipt-sales-and-accounting-verification/RESEARCH.md
@docs/adr/0014-accounting-verification.md
@prisma/schema.prisma
@lib/server/services/customer-sales.ts
@lib/server/services/notifications.ts
@lib/server/services/stock-transfers.ts
@lib/contracts/sales.ts
@tests/helpers/factories.ts
</context>

<tasks>

<task type="auto">
  <name>Closed mismatch enum, review schema, and state machine</name>
  <files>prisma/schema.prisma, prisma/migrations/20260827000001_sale_mismatch_category/migration.sql, lib/server/services/customer-sales.ts, lib/contracts/sales.ts</files>
  <action>Schema: create enum MismatchCategory with PRICE_MISMATCH, QUANTITY_MISMATCH, ITEM_MISMATCH, TOTAL_MISMATCH, RECEIPT_NOT_FOUND, OTHER. Alter SaleAccountingReview.mismatchCategory from String? to MismatchCategory? (add new enum column, backfill existing FLAGGED-derived rows if any, drop old String column or use ALTER COLUMN TYPE USING cast), add receiptPhotoUrl String? (@db.VarChar(1000) or Text) validated as URL, optionally add resolution String? or enum MismatchResolution { RESOLVED_MISTAKEN RESOLVED_CORRECTED } for confirm-correct trace (defer adding if it complicates tracer; at least allow notes to record RESOLVED_MISTAKEN). Keep AccountingReviewStatus as UNVERIFIED, VERIFIED, MISMATCH_REPORTED (already renamed in 02-01). Write additive migration 20260827000001 with safe ALTER TYPE ADD VALUE steps if needed. Service: replace accountingReviewSchema with z.discriminatedUnion("status", [ z.object({ status: z.literal("VERIFIED") }), z.object({ status: z.literal("MISMATCH_REPORTED"), mismatchCategory: z.enum(["PRICE_MISMATCH","QUANTITY_MISMATCH","ITEM_MISMATCH","TOTAL_MISMATCH","RECEIPT_NOT_FOUND","OTHER"]), notes: z.string().trim().min(1).max(1000), receiptPhotoUrl: z.string().url().max(1000).optional() }) ]). Enforce in reviewSale: if status VERIFIED then mismatchCategory and notes and receiptPhotoUrl must be null/absent (clear stale data on confirm), if MISMATCH_REPORTED then mismatchCategory+notes required else throw FLAG_DETAILS_REQUIRED 400. Add state guard via SELECT FOR UPDATE inside Serializable tx: current must be UNVERIFIED to go to VERIFIED or MISMATCH_REPORTED; MISMATCH_REPORTED → VERIFIED allowed only via confirm-correct path in 02-03, so here reject MISMATCH_REPORTED → MISMATCH_REPORTED (idempotent return is allowed to re-save same mismatch from same accounting user), and reject transitions from VERIFIED/VOIDED as terminal (409 INVALID_STATE). Keep assertAccountingStrict (ACCOUNTING_STAFF only) for both branches. Update lib/contracts/sales to re-export mismatchCategory enum and ReviewInput discriminated type.</action>
  <verify>
    <automated>npm run typecheck && npm run test:integration -- tests/integration/sales-mismatch.test.ts --run 2>&1 | grep -E "pass|fail|Tests"</automated>
  </verify>
  <done>Free-text category rejected 400; VERIFIED without mismatch fields succeeds; MISMATCH_REPORTED without category/notes rejected; photoUrl invalid rejected; state guards prevent VERIFIED→MISMATCH and second VERIFIED from changing; enum persisted correctly.</done>
</task>

<task type="auto">
  <name>Mismatch notification fan-out inside transaction</name>
  <files>lib/server/services/notifications.ts, lib/server/services/customer-sales.ts, app/api/sales/[saleId]/review/route.ts</files>
  <action>Update lib/server/services/notifications.ts: extend createNotifications relatedType union to include "SALE" (keep STOCK_TRANSFER, INVENTORY_BALANCE), update caller type accordingly. In customer-sales.ts, add helper notifyUsersForSale(tx, sale, notification) mirroring notifyUsersForTransfer: fetch active ADMIN users (role ADMIN, status ACTIVE) and the single postedBy user (sale.postedById) via tx.user.findMany, dedupe ids, call createNotifications(tx, ids.map(id => ({ userId: id, title, description, type, relatedType: "SALE", relatedId: sale.id, relatedReference: sale.reference }))). Inside reviewSale's Serializable tx after updating SaleAccountingReview to MISMATCH_REPORTED, call notifyUsersForSale with type WARNING, title "Sale mismatch reported", description templated as "${sale.reference} at ${location.code} flagged as ${mismatchCategory} — ${notes.slice(0,140)}" (include branch code via joined Location). Ensure notification creation is inside same tx so it rolls back on failure. For VERIFIED transition (tracer path), no notification is required in this plan (notify on verify is deferred). Ensure POST /api/sales/:saleId/review handler remains ACCOUNTING-only and returns updated review DTO with saleId, status, mismatchCategory, receiptPhotoUrl. Add integration test helper to assert Notification rows created for ADMINs and encoding branch user but not for other branches or STOCK_STAFF.</action>
  <verify>
    <automated>npm run test:integration -- tests/integration/sales-mismatch.test.ts --run -t "notification" 2>&1 | tail -30</automated>
  </verify>
  <done>MISMATCH_REPORTED produces exactly one WARNING per ADMIN + encoding BRANCH_STAFF with relatedType SALE and SALE reference; other roles receive none; notification rows are durable and appear via GET /api/notifications.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| client→API | Crafted mismatchCategory/notes/photoUrl, state bypass |
| service→DB | Notification fan-out to wrong recipients, enum bypass |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-02-01 | Tampering | mismatchCategory free text | medium | mitigate | Prisma MismatchCategory enum + Zod z.enum strict validation 400 |
| T-02-02-02 | Tampering | VERIFIED with stale mismatch fields | medium | mitigate | Service clears mismatchCategory/notes/photoUrl when status VERIFIED, Zod discriminatedUnion |
| T-02-02-03 | Elevation | review state bypass | high | mitigate | SELECT FOR UPDATE + Serializable + UNVERIFIED-only guard, terminal VERIFIED/VOIDED 409 |
| T-02-02-04 | Spoofing | receiptPhotoUrl SSRF/storage abuse | medium | mitigate | z.string().url().max(1000) only, store URL string not binary, no fetch on write |
| T-02-02-05 | Information Disclosure | notification recipients | medium | mitigate | Recipient set is ADMIN + sale.postedById only, deduped, no broadcast |
| T-02-02-SC | Tampering | migration ALTER TYPE | high | mitigate | Hand-authored migration adding enum values with backfill, tested on disposable PG |
</threat_model>

<verification>
Structured mismatch categories are persisted as enum values, state machine rejects invalid transitions with 409, and mismatch notifications are durable within the same transaction as the review.
</verification>

<success_criteria>
Accounting filing MISMATCH_REPORTED with valid closed category creates SaleAccountingReview with that category+notes(+optional photoUrl) and durable SALE notifications for ADMINs and encoding branch; free-text category or missing notes is 400; VERIFIED clears mismatch fields; terminal states are rejected.
</success_criteria>

<output>
Create .planning/phases/02-receipt-sales-and-accounting-verification/02-02-SUMMARY.md when done
</output>
