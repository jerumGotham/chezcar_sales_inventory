---
phase: 01-trusted-foundation-and-data-onboarding
plan: 16
type: execute
wave: 9
depends_on: [01-08, 01-14]
files_modified:
  - components/location-scope-control.tsx
  - app/inventory/page.tsx
  - app/inventory/inventory-client.tsx
autonomous: true
requirements: [REQ-role-authorization]
estimate: { tokens: 28000, raw_tokens: 28000, tasks: 2, confidence: low }
must_haves:
  truths:
    - "Inventory remains capability-gated to Admin, Stock Staff, and valid Branch Staff; Accounting cannot render the page or controls."
    - "Admin controls offer All or one canonical location, Stock Staff is fixed to SR, and Branch Staff is fixed to its persisted branch (D-12)."
    - "The Inventory browser control consumes the same server-derived `LocationScopeDto`, while the API independently re-enforces scope."
  artifacts:
    - { path: "app/inventory/page.tsx", provides: "capability-gated server page/loader" }
    - { path: "app/inventory/inventory-client.tsx", provides: "focused interactive inventory UI" }
    - { path: "components/location-scope-control.tsx", provides: "Admin/Stock/Branch scope control" }
  key_links:
    - { from: "inventory server page", to: "InventoryClient", via: "authorized LocationScopeDto" }
    - { from: "InventoryClient query", to: "/api/inventory", via: "selected/clamped query plus independent API policy" }
  prohibitions:
    - statement: "Inventory controls must not be used to prove Accounting scope feedback or be rendered for Accounting Staff."
      status: unverified
      flagged: true
---

<objective>Render authoritative Inventory scope controls only for roles that actually possess inventory capability.</objective>
<execution_context>@C:/Users/Jerum/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Jerum/.config/opencode/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/01-trusted-foundation-and-data-onboarding/01-CONTEXT.md
@.planning/phases/01-trusted-foundation-and-data-onboarding/01-UI-SPEC.md
@.planning/phases/01-trusted-foundation-and-data-onboarding/01-08-SUMMARY.md
@.planning/phases/01-trusted-foundation-and-data-onboarding/01-14-SUMMARY.md
@app/inventory/page.tsx</context>

<tasks>
<task type="auto">
  <name>Task 1: Split Inventory into an authorized server page and focused client</name>
  <read_first>D-10-D-13; current inventory page; ShellAccessDto/LocationScopeDto; inventory capability and API contract</read_first>
  <files>app/inventory/page.tsx, app/inventory/inventory-client.tsx</files>
  <action>Convert `app/inventory/page.tsx` to an async server page that requires inventory-view, loads persisted shell/location scope, and passes only the browser-safe `LocationScopeDto` to `InventoryClient`. Move the existing interactive query/table state into the focused client without copying stale role constants. Accounting must be rejected before `InventoryClient` or protected data renders.</action>
  <verify><automated>npm run test -- lib/server/shell.test.ts tests/routes/authorization.test.ts && npm run typecheck</automated></verify>
  <done>Inventory has a server authorization/DTO seam and a client bundle free of auth/Prisma policy code.</done>
</task>

<task type="auto">
  <name>Task 2: Clamp Inventory controls to authorized Admin, Stock, and Branch scope</name>
  <read_first>D-12/D-13/D-18; UI-SPEC scope feedback; InventoryClient query/filter behavior; Plan 01-14 hostile API tests</read_first>
  <files>components/location-scope-control.tsx, app/inventory/inventory-client.tsx</files>
  <action>Create `LocationScopeControl` from `LocationScopeDto`: Admin gets enabled All/SR/QC/BL/LU/VC/SP choices; Stock Staff gets static Stock Room (SR); Branch Staff gets static assigned active branch. Do not add an Accounting branch because Accounting cannot reach Inventory. Initialize and clamp applied queries from the DTO, preserve existing filter/page/reset/placeholder behavior, and rely on Plan 01-14 API enforcement for direct requests.</action>
  <verify><automated>npm run test -- lib/server/shell.test.ts tests/routes/authorization.test.ts && npm run typecheck && npm run build</automated><human-check>At phase UAT, inspect Admin/Stock/Branch Inventory controls and confirm Accounting direct navigation is denied while its global AppHeader still says Business-wide.</human-check></verify>
  <done>Only inventory-capable roles see controls, and no control can request broader scope than persisted policy.</done>
</task>
</tasks>

<threat_model>
**OWASP ASVS 5.0 L1 mapping:** V2.2.2 applies because client clamping is usability-only; V8.2.1/V8.2.2/V8.3.1 apply to server page/API capability and data scope; V15.3.1 applies to minimal DTO hydration. Other controls are N/A. High threats below are mitigated, not accepted.
## Trust Boundaries
| Boundary | Description |
|---|---|
| server page→Inventory client | Only authorized browser-safe scope may hydrate. |
| controls→inventory API | Client location input remains untrusted and independently scoped. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-16-01 | Elevation | Accounting Inventory render | high | mitigate | Server capability gate; explicit route/header tests and UAT. |
| T-01-16-02 | Disclosure | client scope control | high | mitigate | Persisted DTO clamping plus Plan 01-14 API enforcement. |
| T-01-16-03 | Tampering | client query | high | mitigate | Server ignores broadening parameters and uses persisted scope. |
| T-01-SC | Tampering | UI dependencies | high | mitigate | Existing approved primitives only. |
</threat_model>

## Artifacts this phase produces
- Symbols: `InventoryClient`, `LocationScopeControl`.
- Files: three focused inventory presentation paths; no API/schema/package changes.

<verification>Shell/route tests, typecheck/build, and end-of-phase role UI checks pass.</verification>
<success_criteria>Inventory scope UI matches D-12 for authorized roles and Accounting scope is proven only in the global header.</success_criteria>
<output>Create `.planning/phases/01-trusted-foundation-and-data-onboarding/01-16-SUMMARY.md` when done.</output>
