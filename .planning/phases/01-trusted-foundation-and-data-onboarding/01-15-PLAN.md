---
phase: 01-trusted-foundation-and-data-onboarding
plan: 15
type: execute
wave: 8
depends_on: [01-07]
files_modified:
  - proxy.ts
  - proxy.test.ts
  - app/access-denied/page.tsx
autonomous: true
requirements: [REQ-role-authorization]
estimate: { tokens: 22000, raw_tokens: 22000, tasks: 2, confidence: low }
must_haves:
  truths:
    - "Missing, expired, inactive, or revoked page sessions go to sign-in with a safe local callback."
    - "Authenticated forbidden page requests show the dedicated access-denied screen with no protected data and one dashboard return action (D-11)."
  artifacts:
    - { path: "proxy.ts", provides: "persisted page capability routing" }
    - { path: "proxy.test.ts", provides: "sign-in versus denial and disclosure proof" }
    - { path: "app/access-denied/page.tsx", provides: "safe fixed denial screen" }
  key_links:
    - { from: "proxy.ts", to: "lib/server/policy/access.ts", via: "page capability decision" }
    - { from: "forbidden decision", to: "/access-denied", via: "fixed URL without protected parameters" }
  prohibitions:
    - statement: "Denied pages must not render or encode protected headers, filters, rows, record identifiers, requested parameters, or retry URLs."
      status: unverified
      flagged: true
---

<objective>Deliver the dedicated fail-closed page denial path independently from shell and inventory presentation work.</objective>
<execution_context>@C:/Users/Jerum/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Jerum/.config/opencode/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/01-trusted-foundation-and-data-onboarding/01-CONTEXT.md
@.planning/phases/01-trusted-foundation-and-data-onboarding/01-UI-SPEC.md
@.planning/phases/01-trusted-foundation-and-data-onboarding/01-07-SUMMARY.md
@proxy.ts
@app/sign-in/page.tsx</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Distinguish unauthenticated and authenticated-forbidden page requests</name>
  <read_first>D-10/D-11; UI-SPEC denial contract; proxy.ts; central policy; sign-in callback validation</read_first>
  <files>proxy.ts, proxy.test.ts</files>
  <behavior>- Missing/expired/inactive/revoked session redirects to `/sign-in` with a validated local callback.
- Authenticated forbidden capability redirects to `/access-denied` without resource/query values.
- Allowed page capability continues without redirect.</behavior>
  <action>Replace hard-coded proxy route clauses with central page capabilities and persisted active User/Location context. Preserve safe local callback validation for unauthenticated requests; authenticated denial must use a fixed `/access-denied` URL. Add direct tests for each session state, all fixed roles, policy-order invariance, callback rejection, and absence of requested records/filters in denial URLs.</action>
  <verify><automated>npm run test -- proxy.test.ts</automated></verify>
  <done>Every page request receives the correct sign-in, denial, or allowed outcome before protected rendering.</done>
</task>

<task type="auto">
  <name>Task 2: Render the fixed protected-data-free denial surface</name>
  <read_first>D-11/D-18; UI-SPEC exact denial copy/layout; PageShell and sign-in page server-component patterns</read_first>
  <files>app/access-denied/page.tsx</files>
  <action>Build a server-component Card with Lucide ShieldX, exact heading/body, and one `Back to Dashboard` Next Link. Render inside the authenticated shell but outside denied business `PageShell` content. Accept no query/record/retry props and expose no denied route details.</action>
  <verify><automated>npm run test -- proxy.test.ts && npm run typecheck && npm run build</automated><human-check>At phase UAT, direct forbidden navigation shows only approved copy, permitted shell content, and Back to Dashboard.</human-check></verify>
  <done>Authenticated users receive an explicit, safe, accessible denial experience.</done>
</task>
</tasks>

<threat_model>
**OWASP ASVS 5.0 L1 mapping:** V7.2.1 applies to backend session verification; V8.2.1/V8.3.1 apply to page-function restriction at the trusted proxy; V15.3.1 applies to returning only fixed non-protected denial fields. Other controls are N/A. High threats below are mitigated, not accepted.
## Trust Boundaries
| Boundary | Description |
|---|---|
| URL/session→proxy | Requested route/parameters and session state are untrusted. |
| proxy→denial page | No protected request context may cross into rendered denial UI. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-15-01 | Elevation | direct forbidden page | high | mitigate | Persisted page capability before rendering. |
| T-01-15-02 | Information Disclosure | denial URL/page | high | mitigate | Fixed URL/copy and tests excluding requested values. |
| T-01-15-03 | Spoofing | unsafe callback | medium | mitigate | Same-origin local callback validation. |
| T-01-SC | Tampering | dependencies | high | mitigate | Existing UI primitives only; no install. |
</threat_model>

## Artifacts this phase produces
- Route/file: `/access-denied`; persisted proxy rules and focused tests.
- No schema, package, or API changes.

<verification>Proxy tests, typecheck/build, and end-of-phase denial check pass.</verification>
<success_criteria>Direct page access fails closed without protected data and matches D-11/D-18.</success_criteria>
<output>Create `.planning/phases/01-trusted-foundation-and-data-onboarding/01-15-SUMMARY.md` when done.</output>
