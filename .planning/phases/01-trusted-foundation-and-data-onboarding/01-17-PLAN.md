---
phase: 01-trusted-foundation-and-data-onboarding
plan: 17
type: execute
wave: 8
depends_on: [01-07]
files_modified:
  - lib/server/internal-user-auth.ts
  - tests/integration/auth-admin-surface.test.ts
autonomous: true
requirements: [REQ-user-management, REQ-role-authorization]
estimate: { tokens: 24000, raw_tokens: 24000, tasks: 2, confidence: low }
must_haves:
  truths:
    - "Pinned Better Auth 1.6.23 create-user/set-user-password primitives are available only through a server-only unmounted instance."
    - "The public auth catch-all continues to expose neither public sign-up nor generic Admin create/list/set-password/revoke operations."
    - "Application `User.status` remains the active/inactive authority; plugin compatibility fields do not create a second lifecycle policy."
  artifacts:
    - { path: "lib/server/internal-user-auth.ts", provides: "unmounted Better Auth Admin-plugin server primitives" }
    - { path: "tests/integration/auth-admin-surface.test.ts", provides: "internal success/public unavailability regression proof" }
  key_links:
    - { from: "Plan 01-09 users service", to: "internalUserAuth.api", via: "owner-authorized createUser/setUserPassword" }
    - { from: "public auth catch-all", to: "lib/server/auth.ts", via: "existing instance without Admin plugin" }
  prohibitions:
    - statement: "The internal Admin-plugin instance must not be mounted, exported to browser code, or used as a generic user-administration HTTP surface."
      status: unverified
      flagged: true
---

<objective>Establish and prove the exact pinned Better Auth internal credential mechanism before account lifecycle routes consume it.</objective>
<execution_context>@C:/Users/Jerum/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Jerum/.config/opencode/gsd-core/templates/summary.md</execution_context>
<context>@.planning/PROJECT.md
@.planning/phases/01-trusted-foundation-and-data-onboarding/01-CONTEXT.md
@.planning/phases/01-trusted-foundation-and-data-onboarding/01-RESEARCH.md
@.planning/phases/01-trusted-foundation-and-data-onboarding/01-06-SUMMARY.md
@.planning/phases/01-trusted-foundation-and-data-onboarding/01-07-SUMMARY.md
@lib/server/auth.ts
@app/api/auth/[...all]/route.ts
@prisma/schema.prisma</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Wire the unmounted Better Auth 1.6.23 Admin primitives</name>
  <read_first>D-13-D-17; Better Auth 1.6.23 Admin plugin docs in RESEARCH; public auth config/catch-all; Plan 01-06 plugin fields</read_first>
  <files>lib/server/internal-user-auth.ts, tests/integration/auth-admin-surface.test.ts</files>
  <behavior>- Internal createUser creates one fixed-role credential record against the shared Prisma adapter.
- Internal setUserPassword replaces credentials without creating another User/Account.
- Internal module is server-only and uses uppercase ADMIN/BRANCH_STAFF configuration.
- Plugin banned fields remain false/null and application status remains authoritative.</behavior>
  <action>Create `internalUserAuth` with the same Prisma adapter/additional fixed fields and Better Auth `1.6.23` `admin()` plugin configured for uppercase owner/default roles. Never pass it to `toNextJsHandler` or client exports. Add PostgreSQL tests for supported `createUser` and `setUserPassword`, exact primary User identity, one Account, and no plugin/application status drift.</action>
  <verify><automated>npm run test:integration -- tests/integration/auth-admin-surface.test.ts</automated></verify>
  <done>The supported credential primitives work server-side without defining a public administration API.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove public sign-up and generic Admin endpoints stay unavailable</name>
  <read_first>public `lib/server/auth.ts`; `app/api/auth/[...all]/route.ts`; internal auth module; Better Auth 1.6.23 Admin endpoint names</read_first>
  <files>tests/integration/auth-admin-surface.test.ts</files>
  <behavior>- Public sign-up is unavailable.
- Public generic Admin create/list/set-password/revoke endpoint requests are unavailable.
- Import graph shows the catch-all uses only public `auth`, never `internalUserAuth`.
- Internal primitive calls remain possible only from server service code.</behavior>
  <action>Invoke the unchanged public catch-all handler directly for sign-up and every generic Admin operation used by this phase and assert none is routable/successful and no database mutation occurs. Add an import-boundary assertion that `app/api/auth/[...all]/route.ts` imports only `lib/server/auth.ts`. Do not modify or broaden the catch-all to satisfy tests.</action>
  <verify><automated>npm run test:integration -- tests/integration/auth-admin-surface.test.ts && npm run typecheck</automated></verify>
  <done>The exact internal mechanism is usable by Plan 01-09 while its generic HTTP surface remains absent.</done>
</task>
</tasks>

<threat_model>
**OWASP ASVS 5.0 L1 mapping:** V6.2.2 applies to supported password lifecycle, V7.2.1/V7.4.1 to backend credential/session behavior, V8.2.1/V8.3.1 to preventing generic administrative function exposure, and V15.3.1 to narrow outputs. Other controls are N/A. High threats below are mitigated, not accepted.
## Trust Boundaries
| Boundary | Description |
|---|---|
| lifecycle service→internal auth | Only trusted server code may invoke credential primitives. |
| browser→public catch-all | Generic Admin and sign-up operations must remain unavailable. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-17-01 | Elevation | generic Admin routes | high | mitigate | Unmounted instance and direct public-handler unavailability tests. |
| T-01-17-02 | Elevation | public sign-up | high | mitigate | Existing disabled sign-up plus direct regression test. |
| T-01-17-03 | Tampering | dual status authority | high | mitigate | Application status is authoritative; plugin fields fixed and tested. |
| T-01-17-04 | Disclosure | credentials | high | mitigate | Server-only primitives and no password response/log assertions. |
| T-01-SC | Tampering | dependencies | high | mitigate | Existing pinned Better Auth 1.6.23; no install/upgrade. |
</threat_model>

## Artifacts this phase produces
- Symbol: `internalUserAuth`.
- Test: pinned internal primitive and public-surface regression matrix.
- No public route, client export, package, or schema change.

<verification>Internal auth integration tests and strict typecheck pass.</verification>
<success_criteria>Plan 01-09 has supported server-only credential primitives and generic public auth administration remains unavailable.</success_criteria>
<output>Create `.planning/phases/01-trusted-foundation-and-data-onboarding/01-17-SUMMARY.md` when done.</output>
