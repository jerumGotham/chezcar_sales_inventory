---
phase: 01-trusted-foundation-and-data-onboarding
plan: 14
type: execute
wave: 8
depends_on: [01-07]
files_modified:
  - app/api/inventory/route.ts
  - tests/routes/authorization.test.ts
  - lib/server/catalog.ts
  - tests/integration/inventory-scope.test.ts
autonomous: true
requirements: [REQ-role-authorization]
estimate: { tokens: 34000, raw_tokens: 34000, tasks: 2, confidence: low }
must_haves:
  truths:
    - "Inventory independently requires the named inventory capability before query parsing or protected reads."
    - "Branch Staff cannot escape its persisted active branch; Stock Staff remains SR-scoped; Admin may use All or one canonical location."
    - "Accounting has no inventory capability in Phase 1 and direct inventory requests return 403 without rows."
  artifacts:
    - { path: "app/api/inventory/route.ts", provides: "named-capability inventory boundary" }
    - { path: "lib/server/catalog.ts", provides: "typed persisted location-scope resolution" }
    - { path: "tests/integration/inventory-scope.test.ts", provides: "hostile parameter and four-role proof" }
  key_links:
    - { from: "inventory route", to: "requireCapability", via: "inventory:view before service" }
    - { from: "catalog query", to: "PersistedAccessContext.location", via: "resolveLocationScope before client filters" }
  prohibitions:
    - statement: "The Business-wide Accounting presentation label must not grant inventory capability or broaden catalog queries."
      status: unverified
      flagged: true
---

<objective>Close the inventory API and data-scope boundary with hostile direct-request proof for all four roles.</objective>
<execution_context>@C:/Users/Jerum/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Jerum/.config/opencode/gsd-core/templates/summary.md</execution_context>
<context>@.planning/PROJECT.md
@.planning/phases/01-trusted-foundation-and-data-onboarding/01-CONTEXT.md
@.planning/phases/01-trusted-foundation-and-data-onboarding/01-07-SUMMARY.md
@app/api/inventory/route.ts
@lib/server/catalog.ts</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Migrate Inventory to its named persisted capability</name>
  <read_first>D-10-D-13; 01-07 policy/authorization exports and route tests; inventory route; catalog query schema</read_first>
  <files>app/api/inventory/route.ts, tests/routes/authorization.test.ts</files>
  <behavior>- Missing/inactive/revoked session returns 401 before service.
- Accounting and other disallowed principals return 403 without protected payload.
- Admin, Stock Staff, and valid Branch Staff enter the service with persisted context only.</behavior>
  <action>Replace the inventory allowed-role array with the named inventory-view capability from Plan 01-07. Extend the direct-handler suite to the fifth route family, asserting guard-before-query/service order, stable 401/403 envelopes, no data on denial, and explicit Accounting denial. No current sensitive route may retain a direct role-array policy list after this task.</action>
  <verify><automated>npm run test -- tests/routes/authorization.test.ts && npm run typecheck</automated></verify>
  <done>Inventory joins all current sensitive reads behind independent named server authorization.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make persisted inventory scope invariant to hostile parameters</name>
  <read_first>D-12/D-13; current catalog scope derivation; canonical locations; Plan 01-13 database/request helpers</read_first>
  <files>lib/server/catalog.ts, tests/integration/inventory-scope.test.ts</files>
  <behavior>- Branch Staff receives only its persisted branch despite all/sibling/SR/duplicate/reordered parameters.
- Stock Staff receives SR only regardless of supplied location.
- Admin may request all or one canonical active location.
- Accounting, missing assignment, and invalid/inactive assignment receive denial and no rows.</behavior>
  <action>Replace sentinel scope with typed `resolveLocationScope` from persisted context and canonicalize duplicate/reordered query handling before Prisma filters. Test distinct marker records in SR and two branches for all four roles, including explicit Accounting 403, and assert denied responses contain no protected rows.</action>
  <verify><automated>npm run test:integration -- tests/integration/inventory-scope.test.ts</automated></verify>
  <done>Caller parameters and Accounting's presentation scope cannot broaden inventory data access.</done>
</task>
</tasks>

<threat_model>
**OWASP ASVS 5.0 L1 mapping:** V7.2.1 covers backend session verification; V8.2.1/V8.2.2/V8.3.1 cover function/data restriction and trusted-service scope enforcement. Other controls are N/A to this focused inventory boundary. High/critical items below are mitigated, not accepted.
## Trust Boundaries
| Boundary | Description |
|---|---|
| session/query→inventory route | Principal and all caller location/query values are untrusted. |
| persisted context→Prisma filter | Only canonical policy scope may determine visible rows. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-14-01 | Elevation/Disclosure | inventory capability | high | mitigate | Named capability and explicit Accounting/direct-request denial tests. |
| T-01-14-02 | Tampering/Disclosure | location parameters | high | mitigate | Persisted typed scope and duplicate/reordered hostile matrix. |
| T-01-14-03 | Disclosure | denied response | high | mitigate | Stable error-only envelopes and marker-row absence assertions. |
| T-01-SC | Tampering | dependencies | high | mitigate | No install; existing locked stack only. |
</threat_model>

## Artifacts this phase produces
- Symbols: `resolveLocationScope`; expanded authorization route matrix.
- Files: inventory route/catalog plus focused route/integration tests.

<verification>Direct route tests, hostile PostgreSQL scope tests, and typecheck pass.</verification>
<success_criteria>Inventory is capability-gated and persisted-scope invariant for Admin, Stock, Branch, and denied Accounting requests.</success_criteria>
<output>Create `.planning/phases/01-trusted-foundation-and-data-onboarding/01-14-SUMMARY.md` when done.</output>
