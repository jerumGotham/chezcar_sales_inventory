---
phase: 01-trusted-foundation-and-data-onboarding
plan: 06
type: execute
wave: 6
depends_on: [01-05, 01-13]
files_modified:
  - package.json
  - prisma/schema.prisma
  - prisma/migrations/20260825_trusted_foundation/migration.sql
  - tests/integration/migration.test.ts
  - prisma/seed.mjs
  - lib/server/services/catalog-reset.ts
  - lib/server/services/catalog-reset.test.ts
  - tests/integration/seed.test.ts
autonomous: true
requirements: [REQ-data-onboarding, REQ-role-authorization, REQ-user-management]
estimate: { tokens: 60000, raw_tokens: 60000, tasks: 3, confidence: low }
must_haves:
  truths:
    - "A fresh disposable database applies every committed migration before seed verification."
    - "Seed/reset loads exact reviewed locations/products/prices/opening balances transactionally and reruns equivalently (D-03-D-05)."
    - "Production, the development bind mount, and unknown database targets are refused before the first reset write."
    - "Database constraints reject role/location nullability violations and a second ADMIN; exactly one seeded owner Admin remains."
    - "The additive schema supports the pinned Better Auth 1.6.23 internal Admin plugin without making plugin `banned` fields a second application account-status authority."
  artifacts:
    - { path: "prisma/schema.prisma", provides: "credentialSetupRequired and pinned Better Auth 1.6.23 Admin-plugin persistence fields" }
    - { path: "prisma/migrations/20260825_trusted_foundation/migration.sql", provides: "additive auth fields, checks, and singleton-Admin guard" }
    - { path: "lib/server/services/catalog-reset.ts", provides: "transactional development/test catalog reload" }
  key_links:
    - { from: "opening-catalog.json", to: "prisma/seed.mjs", via: "validated fixture import" }
    - { from: "catalog-reset.ts", to: "Prisma transaction", via: "positive target gate before writes" }
  prohibitions:
    - statement: "Catalog reload must not be invocable through HTTP, in production, against an unknown target, or by globally resetting the database."
      status: unverified
      flagged: true
---

<objective>Commit and apply the additive schema, then transactionally load the approved canonical opening data with structural safety gates.</objective>
<execution_context>@C:/Users/Jerum/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Jerum/.config/opencode/gsd-core/templates/summary.md</execution_context>
<context>@.planning/PROJECT.md
@.planning/phases/01-trusted-foundation-and-data-onboarding/01-CONTEXT.md
@.planning/phases/01-trusted-foundation-and-data-onboarding/01-RESEARCH.md
@.planning/phases/01-trusted-foundation-and-data-onboarding/01-PATTERNS.md
@.planning/phases/01-trusted-foundation-and-data-onboarding/01-05-SUMMARY.md
@.planning/phases/01-trusted-foundation-and-data-onboarding/01-13-SUMMARY.md
@prisma/schema.prisma
@prisma/seed.mjs
@docs/DATABASE.md</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Define canonical account state and database invariants</name>
  <reversibility rating="costly">The additive schema and fixture mapping are inherited by every later workflow and require a coordinated migration to change.</reversibility>
  <read_first>D-03/D-13-D-17; schema.prisma; initial migration SQL; opening-catalog.json; 01-RESEARCH Existing Canonical Values</read_first>
  <files>prisma/schema.prisma</files>
  <behavior>- User credential prompt state is persisted as `credentialSetupRequired`.
- Better Auth 1.6.23 Admin-plugin fields `banned`, `banReason`, `banExpires`, and Session `impersonatedBy` exist additively; `User.status` remains the sole application active/inactive authority.
- Role/location application validation can distinguish no assignment from one assignment.
- Primary identity remains the Better Auth/Prisma User ID; no generalized admin-assignment model is introduced.</behavior>
  <action>Add `credentialSetupRequired Boolean @default(false)` plus the Better Auth 1.6.23 Admin-plugin compatibility fields `banned Boolean @default(false)`, nullable `banReason`, nullable `banExpires`, and nullable `Session.impersonatedBy`; retain the existing fixed uppercase role enum/relations. Document in schema comments/tests that application activation uses `User.status` only and lifecycle services keep plugin `banned` false/null, preventing two status authorities. Do not add custom permissions, a second admin-role model, workbook-shaped columns, or future sales/movement entities. Prepare the schema for a hand-reviewed SQL role/location check and singleton ADMIN partial unique index in Task 2.</action>
  <acceptance_criteria>`prisma format`/generate accepts the schema; new fields exactly cover credential prompt plus pinned plugin compatibility; existing IDs, fixed role enum, and `User.status` remain authoritative.</acceptance_criteria>
  <verify><automated>npm run prisma:generate && npm run typecheck</automated></verify>
  <done>The additive Prisma model represents first-login state without widening Phase 1 scope.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Generate, commit, and prove the additive migration</name>
  <precondition>An approved disposable/development DATABASE_URL is set and `assertDisposableDatabaseUrl` accepts it; never use an unknown or production database.</precondition>
  <reversibility rating="costly">Undoing a committed, applied schema requires an additive corrective migration.</reversibility>
  <read_first>schema.prisma; initial migration SQL; package.json db:migrate script; tests/helpers/database.ts; docs/DATABASE.md local data warning</read_first>
  <files>prisma/migrations/20260825_trusted_foundation/migration.sql, tests/integration/migration.test.ts</files>
  <action>Run the checked-in `npm run db:migrate -- --name trusted_foundation` workflow against the approved disposable/development target to generate the migration. Hand-review the additive credential/plugin columns and add SQL constraints: ADMIN/ACCOUNTING_STAFF require null location, STOCK_STAFF/BRANCH_STAFF require non-null location, and a partial unique index permits at most one ADMIN. Then create a fresh disposable database and apply all committed migrations with Prisma migrate deploy. Never use db push with data-loss acceptance and never reset an unknown or production target.</action>
  <acceptance_criteria>The committed migration is additive; a fresh disposable database applies initial plus trusted-foundation migrations; SQL probes in migration.test.ts verify plugin field defaults and reject role/nullability violations and a second ADMIN.</acceptance_criteria>
  <verify><automated>npm run test:integration -- tests/integration/migration.test.ts</automated></verify>
  <done>Schema application is proven on a fresh disposable database before seed/reset verification continues.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Transactionally seed and reload reviewed opening data</name>
  <read_first>D-03-D-05; opening-catalog.json; current seed.mjs; catalog-reset patterns; database helper; applied migration</read_first>
  <files>package.json, prisma/seed.mjs, lib/server/services/catalog-reset.ts, lib/server/services/catalog-reset.test.ts, tests/integration/seed.test.ts</files>
  <behavior>- Empty/invalid fixture performs no writes.
- Seed/reset atomically replaces only catalog/opening balances, preserving auth/users.
- Exact six locations and approved products/prices/balances exist after one and two runs.
- Production/unknown/development-bind-mount targets refuse before writes.
- Owner Admin seed remains exactly one and never commits credentials.</behavior>
  <action>Replace inline catalog fixtures with validated `opening-catalog.json`; keep environment-driven owner Admin provisioning but fail if a different ADMIN exists. Implement `reloadOpeningCatalog` as one short Prisma transaction guarded by `NODE_ENV`, explicit `ALLOW_CATALOG_RESET`, and disposable/development target identity. Add developer CLI script `db:catalog:reload`; no route handler. Test rollback injection, rerun equivalence, preservation of users/auth, and all refusal paths.</action>
  <acceptance_criteria>Integration tests apply migrations fresh, seed twice, compare canonical rows, inject a mid-transaction failure with zero partial replacement, and prove all forbidden targets remain unchanged.</acceptance_criteria>
  <verify><automated>npm run test -- lib/server/services/catalog-reset.test.ts && npm run test:integration -- tests/integration/seed.test.ts</automated></verify>
  <done>Development/test can safely and repeatably load the approved opening data; production has no reset path.</done>
</task>
</tasks>

<threat_model>
**OWASP ASVS 5.0 L1 mapping:** V2.2.1 and V2.2.2 apply to fixture/environment validation at the trusted reset service; V7.4.2 applies to schema support for account-disable session termination; V8.1.1 records fixed role/location rules. Other controls are N/A to this migration/reload slice. High/critical items below are mitigated, not accepted.
## Trust Boundaries
| Boundary | Description |
|---|---|
| fixture→database | Reviewed JSON becomes canonical persisted rows. |
| CLI environment→reset service | Environment and target identity govern destructive writes. |
| account service→User constraints | Invalid scope or second Admin must fail closed. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-01 | Elevation | cross-branch scope | high | mitigate | DB nullability check plus 01-07 related-location policy. |
| T-01-02 | Elevation | non-Admin user APIs | high | mitigate | 01-09 service guards. |
| T-01-03 | Elevation | retained sessions | high | mitigate | 01-09 atomic transaction. |
| T-01-04 | Tampering | formula/reference poisoning | high | mitigate | Database consumes only reviewed/hash-validated canonical JSON. |
| T-01-05 | Tampering/DoS | production reset | critical | mitigate | No HTTP route; positive env/target allowlist; pre-write refusal; transaction. |
| T-01-06 | Elevation | second Admin | high | mitigate | Partial unique index and seed/service checks. |
| T-01-07 | Elevation | Better Auth overexposure | high | mitigate | Auth schema remains fixed; 01-09 exposes only narrow APIs. |
| T-01-08 | Information Disclosure | temp password | high | mitigate | Seed credentials remain environment-only and unlogged. |
| T-01-SC | Tampering | package tree | high | mitigate | No dependency changes. |
</threat_model>

## Artifacts this phase produces
- Field: `User.credentialSetupRequired`.
- Symbols: `reloadOpeningCatalog`, target/fixture validators.
- CLI script: `db:catalog:reload`; env gate `ALLOW_CATALOG_RESET`.
- Files: additive migration, migration/reset/seed tests, reset service, modified schema/seed/package script.

<verification>Fresh migration, repeat seed, rollback, refusal, Prisma generation, and typecheck commands pass on an approved disposable database.</verification>
<success_criteria>Canonical opening data is durable and equivalent on rerun, invalid input leaves no partial rows, and destructive scope is structurally non-production.</success_criteria>
<output>Create `.planning/phases/01-trusted-foundation-and-data-onboarding/01-06-SUMMARY.md` when done.</output>
