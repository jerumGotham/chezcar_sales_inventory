---
phase: 01-trusted-foundation-and-data-onboarding
plan: 07
type: execute
wave: 7
depends_on: [01-06]
files_modified:
  - lib/server/policy/access.ts
  - lib/server/authorization.ts
  - lib/server/authorization.test.ts
  - tests/routes/authorization.test.ts
  - app/api/dashboard/route.ts
  - app/api/customers/route.ts
  - app/api/customer-orders/route.ts
  - app/api/products/route.ts
autonomous: true
requirements: [REQ-role-authorization]
estimate: { tokens: 42000, raw_tokens: 42000, tasks: 2, confidence: low }
must_haves:
  truths:
    - "Missing/revoked/inactive sessions return 401/sign-in behavior; authenticated wrong-role/location access returns denial without protected data."
    - "Missing, extra, or incompatible assignments fail closed: Stock Staff only SR, Branch Staff exactly one active branch, Admin/Accounting none (D-13)."
    - "Every Phase 1 sensitive API independently authorizes; hidden navigation is never authority (D-10)."
  artifacts:
    - { path: "lib/server/policy/access.ts", provides: "fixed capability and scope matrix" }
    - { path: "lib/server/authorization.test.ts", provides: "closed-session and assignment matrix" }
  key_links:
    - { from: "route handlers", to: "requireCapability", via: "persisted active User context" }
    - { from: "four current read handlers", to: "requireCapability", via: "named capability before service execution" }
  prohibitions:
    - statement: "Client filters, menu visibility, request ordering, or caller-supplied role/location values must not broaden persisted authorization scope."
      status: unverified
      flagged: true
---

<objective>Establish the fixed persisted capability boundary and migrate four current sensitive reads to it.</objective>
<execution_context>@C:/Users/Jerum/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Jerum/.config/opencode/gsd-core/templates/summary.md</execution_context>
<context>@.planning/PROJECT.md
@.planning/phases/01-trusted-foundation-and-data-onboarding/01-CONTEXT.md
@.planning/phases/01-trusted-foundation-and-data-onboarding/01-RESEARCH.md
@.planning/phases/01-trusted-foundation-and-data-onboarding/01-PATTERNS.md
@.planning/phases/01-trusted-foundation-and-data-onboarding/01-06-SUMMARY.md
@lib/server/authorization.ts
@lib/server/catalog.ts
@app/api/inventory/route.ts</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Evaluate fixed capabilities from persisted active-user scope</name>
  <read_first>D-10-D-13; authorization.ts; schema.prisma/migration; 01-RESEARCH central policy; current proxy and menu as non-authoritative consumers</read_first>
  <files>lib/server/policy/access.ts, lib/server/authorization.ts, lib/server/authorization.test.ts</files>
  <behavior>- No session, missing user, inactive user, and revoked session authenticate as 401.
- Authenticated disallowed capability is 403.
- ADMIN/ACCOUNTING with location, STOCK_STAFF without SR or with extra/branch scope, and BRANCH_STAFF without exactly one active branch fail closed.
- Equivalent query/policy-check permutations yield the same decision.</behavior>
  <action>Define `AccessResource`, `AccessAction`, `Capability`, `PersistedAccessContext`, `evaluateAccess`, and `validatePersistedAssignment`. Replace allowed-role arrays at the shared boundary with `requireCapability(headers, capability)` while retaining stable 401/403 envelopes. Reload User plus Location code/type/isActive on each sensitive request. Make checks pure and order-invariant; never trust session custom fields or request scope over persisted rows.</action>
  <acceptance_criteria>Matrix/table tests cover all four roles, missing/extra/incompatible/inactive locations, session states, wrong capability, and permutations; every invalid state yields no AuthContext.</acceptance_criteria>
  <verify><automated>npm run test -- lib/server/authorization.test.ts</automated></verify>
  <done>One persisted fixed policy produces deterministic fail-closed authorization outcomes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate and prove four current sensitive reads cross the persisted guard</name>
  <read_first>access.ts/authorization.ts; all five route handlers; docs/API.md role matrix; ROADMAP Phase 1 boundary</read_first>
  <files>app/api/dashboard/route.ts, app/api/customers/route.ts, app/api/customer-orders/route.ts, app/api/products/route.ts, tests/routes/authorization.test.ts</files>
  <behavior>- Each route invokes persisted `requireCapability` before parsing/reading protected records.
- 401/403 bodies contain no protected payload.
- Roles outside a route capability cannot retrieve data by direct HTTP call.</behavior>
  <action>Migrate existing allowed-role-array calls in `app/api/dashboard/route.ts`, `app/api/customers/route.ts`, `app/api/customer-orders/route.ts`, and `app/api/products/route.ts` to their named `requireCapability` values. Add the parameterized direct-handler suite for those four families, keeping each guard before query parsing/service execution and preserving the current mock-versus-Prisma data source.</action>
  <acceptance_criteria>Focused route tests prove missing session 401, wrong role 403, allowed role success, guard-before-service ordering, and denied bodies containing only stable error envelopes for all four migrated route families.</acceptance_criteria>
  <verify><automated>npm run test -- tests/routes/authorization.test.ts && npm run typecheck</automated></verify>
  <done>Every current sensitive HTTP read has independent server authorization.</done>
</task>

</tasks>

<threat_model>
**OWASP ASVS 5.0 L1 mapping:** V7.2.1 applies to backend session verification; V8.1.1 documents the fixed function/data matrix; V8.2.1, V8.2.2, and V8.3.1 apply to named route capabilities, persisted object scope, and trusted-service enforcement. Other controls are N/A to this policy slice. High/critical items below are mitigated, not accepted.
## Trust Boundaries
| Boundary | Description |
|---|---|
| session cookie→authorization | Cookie identity is revalidated against persisted active User/Location. |
| query/path→service | Caller scope is untrusted and cannot broaden access. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-01 | Elevation/Disclosure | cross-branch scope | high | mitigate | This plan establishes persisted context; Plan 01-14 adds typed Inventory scope and hostile integration matrix. |
| T-01-02 | Elevation | direct non-Admin user APIs | high | mitigate | Capability service is reused by 01-09 routes. |
| T-01-03 | Elevation | retained sessions | high | mitigate | Database session validation here; deletion transaction in 01-09. |
| T-01-04 | Tampering | workbook poisoning | high | mitigate | Reads only 01-06 canonical persisted rows. |
| T-01-05 | Tampering/DoS | production reset | critical | mitigate | No reset surface; 01-06 guarded CLI only. |
| T-01-06 | Elevation | second Admin | high | mitigate | DB constraint plus 01-09 service allowlist. |
| T-01-07 | Elevation | Better Auth overexposure | high | mitigate | Auth resolution only; application capabilities protect data routes. |
| T-01-08 | Information Disclosure | temp password | high | mitigate | Error mapper never serializes credentials. |
| T-01-SC | Tampering | dependencies | high | mitigate | No installs. |
</threat_model>

## Artifacts this phase produces
- Symbols: `AccessResource`, `AccessAction`, `Capability`, `PersistedAccessContext`, `evaluateAccess`, `validatePersistedAssignment`, `requireCapability`.
- Files: policy/authorization modules, unit/direct-route tests, and four modified read handlers.
- No new CLI flags or schema fields.

<verification>Authorization unit/route tests and typecheck pass for the persisted policy plus four migrated route families.</verification>
<success_criteria>Every role/location/session edge fails closed and the four migrated handlers authorize independently; inventory scope closure follows in Plan 01-14.</success_criteria>
<output>Create `.planning/phases/01-trusted-foundation-and-data-onboarding/01-07-SUMMARY.md` when done.</output>
