---
phase: 01-trusted-foundation-and-data-onboarding
plan: 13
type: execute
wave: 3
depends_on: [01-02]
files_modified:
  - tests/helpers/database.ts
  - tests/helpers/database.test.ts
  - tests/helpers/factories.ts
  - tests/helpers/requests.ts
autonomous: true
requirements: [REQ-data-onboarding, REQ-role-authorization, REQ-user-management]
estimate: { tokens: 26000, raw_tokens: 26000, tasks: 2, confidence: low }
must_haves:
  truths:
    - "Integration setup accepts only a separately named disposable PostgreSQL 17 target and rejects the development bind mount, production, and unknown URLs before destructive work."
    - "Later authorization/user tests create canonical fixed-role/location users, sessions, and direct requests without importing prototype fixtures."
  artifacts:
    - { path: "tests/helpers/database.ts", provides: "protected disposable PostgreSQL lifecycle" }
    - { path: "tests/helpers/factories.ts", provides: "canonical role/location/session fixtures" }
    - { path: "tests/helpers/requests.ts", provides: "direct route request construction" }
  key_links:
    - { from: "integration suites", to: "withDisposableDatabase", via: "positive target assertion before migration/cleanup" }
    - { from: "route tests", to: "createAuthFixture/createRequest", via: "persisted principal and hostile input" }
  prohibitions:
    - statement: "Test infrastructure must not inspect, reset, or reuse the checked-in development bind mount or page-local mock users."
      status: unverified
      flagged: true
---

<objective>Provide the isolated database and direct-request harness required by later persistence, authorization, and user-lifecycle plans.</objective>
<execution_context>@C:/Users/Jerum/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Jerum/.config/opencode/gsd-core/templates/summary.md</execution_context>
<context>@.planning/PROJECT.md
@.planning/phases/01-trusted-foundation-and-data-onboarding/01-CONTEXT.md
@.planning/phases/01-trusted-foundation-and-data-onboarding/01-VALIDATION.md
@.planning/phases/01-trusted-foundation-and-data-onboarding/01-02-SUMMARY.md
@docker-compose.yml
@lib/server/prisma.ts
@prisma/schema.prisma</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Refuse every non-disposable PostgreSQL target</name>
  <read_first>01-VALIDATION Test Infrastructure/Wave 0; docker-compose.yml; docs/DATABASE.md local-data warning; lib/server/prisma.ts</read_first>
  <files>tests/helpers/database.ts, tests/helpers/database.test.ts</files>
  <behavior>- Dedicated test marker/name/port is accepted.
- Checked-in development database name/port, production mode, malformed URL, and arbitrary target fail before migration or cleanup.
- Lifecycle starts PostgreSQL 17, applies migrations serially, and always cleans up its separate target.</behavior>
  <action>Implement `assertDisposableDatabaseUrl` and `withDisposableDatabase` with positive environment, database-name, container-name, and port checks. Add lifecycle hooks for a separately named PostgreSQL 17 target, serial migrate-deploy execution, cleanup, and failure-safe teardown. Tests must prove every refusal occurs before a destructive command and never access `data/`.</action>
  <verify><automated>npm run test -- tests/helpers/database.test.ts && npm run typecheck</automated></verify>
  <done>Integration tests cannot target the known development bind mount, production, or an unrecognized database.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build canonical persisted actor and request factories</name>
  <read_first>D-03/D-13-D-17; schema.prisma; authorization.ts; app/api/inventory/route.ts; database helper</read_first>
  <files>tests/helpers/factories.ts, tests/helpers/requests.ts</files>
  <behavior>- Factories create SR plus active branches and all four fixed role/location combinations.
- Factories can create active/inactive users and valid/expired/revoked sessions.
- Requests support duplicate/reordered query parameters and direct route bodies/headers.</behavior>
  <action>Export `createAuthFixture` and `createRequest` plus typed location/user/session builders using Prisma business keys and Better Auth session rows. Encode D-13 valid defaults while allowing explicitly requested invalid persisted states for fail-closed tests. Do not import mock UI users, hard-coded prototype roles, or client auth helpers.</action>
  <verify><automated>npm run typecheck && npm run test -- tests/helpers/database.test.ts</automated></verify>
  <done>Later tests can exercise real persisted identity, scope, session, and hostile direct-request cases deterministically.</done>
</task>
</tasks>

<threat_model>
**OWASP ASVS 5.0 L1 mapping:** V2.2.1/V2.2.2 apply to positive environment/URL validation at the trusted harness; V7.2.1 applies to backend session fixtures; V8.1.1 applies to documented fixed-role/location fixtures. Other controls are N/A to test infrastructure. High/critical items below are mitigated, not accepted.
## Trust Boundaries
| Boundary | Description |
|---|---|
| harness environment→PostgreSQL | A destructive lifecycle may cross only to a positively identified disposable target. |
| test fixture→authorization | Persisted actors/sessions must represent canonical or deliberately invalid states explicitly. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-13-01 | Tampering/DoS | database lifecycle | critical | mitigate | Positive target checks before commands, separate container/database, refusal tests. |
| T-01-13-02 | Spoofing | auth factories | high | mitigate | Persist real Better Auth session/User rows and typed actor identities. |
| T-01-13-03 | Elevation | prototype fixture drift | high | mitigate | Fixed canonical role/location builders; no page-local fixtures. |
| T-01-SC | Tampering | package installs | high | mitigate | No package installation; uses Plan 01-02's approved lockfile. |
</threat_model>

## Artifacts this phase produces
- Symbols: `assertDisposableDatabaseUrl`, `withDisposableDatabase`, `createAuthFixture`, `createRequest`, typed persisted factories.
- Files: four test-helper paths in frontmatter; no production routes, fields, or packages.

<verification>Database refusal tests and strict typecheck pass under Node 20.</verification>
<success_criteria>Every later database/request suite has a safe, canonical, reusable harness in four cohesive files.</success_criteria>
<output>Create `.planning/phases/01-trusted-foundation-and-data-onboarding/01-13-SUMMARY.md` when done.</output>
