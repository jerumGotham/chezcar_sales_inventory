---
phase: 01-trusted-foundation-and-data-onboarding
plan: 10
type: execute
wave: 9
depends_on: [01-09]
files_modified:
  - lib/server/services/users.ts
  - app/api/credential-setup/route.ts
  - tests/integration/credential-setup.test.ts
  - app/sign-in/sign-in-form.tsx
  - components/credential-setup-dialog.tsx
autonomous: true
requirements: [REQ-user-management, REQ-role-authorization]
estimate: { tokens: 48000, raw_tokens: 48000, tasks: 2, confidence: low }
must_haves:
  truths:
    - "New/reset credentials prompt after successful sign-in; change or skip consumes the prompt until a later reset (D-15)."
    - "Missing/inactive/revoked session cannot read or consume credential state."
    - "Password values never appear in URL, response, banner, log, query cache, or page text."
    - "First-login empty, loading, error, populated/partial, overflow, zero-one-many where applicable, and long-text behavior follows UI-SPEC."
  artifacts:
    - { path: "app/api/credential-setup/route.ts", provides: "authenticated prompt-state/change/skip API" }
    - { path: "components/credential-setup-dialog.tsx", provides: "blocking accessible first-login dialog" }
  key_links:
    - { from: "sign-in-form.tsx", to: "/api/credential-setup", via: "post-auth persisted prompt query" }
    - { from: "credential setup service", to: "User.credentialSetupRequired", via: "change/skip/reset state machine" }
  prohibitions:
    - statement: "Temporary or replacement passwords must not be generated in the browser, echoed after submit, logged, placed in URLs, or represented as shareable online output."
      status: unverified
      flagged: true
---

<objective>Complete the temporary-credential state machine and approved first-login prompt without credential leakage.</objective>
<execution_context>@C:/Users/Jerum/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Jerum/.config/opencode/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/01-trusted-foundation-and-data-onboarding/01-CONTEXT.md
@.planning/phases/01-trusted-foundation-and-data-onboarding/01-UI-SPEC.md
@.planning/phases/01-trusted-foundation-and-data-onboarding/01-PATTERNS.md
@.planning/phases/01-trusted-foundation-and-data-onboarding/01-09-SUMMARY.md
@app/sign-in/sign-in-form.tsx
@lib/server/auth.ts</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Prove credential prompt change/skip/reset transitions</name>
  <read_first>D-15-D-17; UI-SPEC first-login contract; users service; auth.ts; authorization policy; Better Auth password API research</read_first>
  <files>lib/server/services/users.ts, app/api/credential-setup/route.ts, tests/integration/credential-setup.test.ts</files>
  <behavior>- GET returns only whether the active current user requires setup.
- POST change validates current/new/confirmation through Better Auth, changes password, consumes prompt, and invalidates other sessions as appropriate.
- POST skip consumes prompt without claiming password changed.
- Repeated skip/change is idempotent final state; later Admin reset re-arms once.
- Missing/inactive/revoked session is 401 and no state changes.</behavior>
  <action>Add current-user service methods and narrow GET/POST route with discriminated `action: change|skip`. Require persisted active identity and stable error envelopes. Use Better Auth for credential verification/change, clear `credentialSetupRequired` only on successful change or explicit skip, and never return password fields. Add integration sequence reset→login→skip/change→login→reset including retries and failures.</action>
  <acceptance_criteria>Tests prove the exact state transitions, current-password failure leaves state unchanged, later reset re-arms, and serialized bodies/log captures contain no submitted password value.</acceptance_criteria>
  <verify><automated>npm run test:integration -- tests/integration/credential-setup.test.ts</automated></verify>
  <done>The server-backed prompt state is deterministic, authenticated, and non-leaking.</done>
</task>

<task type="auto">
  <name>Task 2: Gate safe callback navigation with the accessible credential dialog</name>
  <read_first>UI-SPEC copy, first-login, responsive/accessibility, and UI Considerations rows; sign-in page callback validation; sign-in-form.tsx; Dialog/Input/Button primitives</read_first>
  <files>app/sign-in/sign-in-form.tsx, components/credential-setup-dialog.tsx</files>
  <action>After successful email sign-in, query credential state before navigation. If required, show a blocking Dialog with exact title/body/actions and Current/New/Confirm fields; change and skip continue only to the already validated local callback. Implement blank defaults, loading labels, field/server errors with retained non-secret context, first-invalid focus, aria-invalid/alert/status, in-flight close prevention, Escape/focus return, reduced motion, 320px layout, and dark tokens. Clear password component state on close/success and do not put it in React Query.</action>
  <acceptance_criteria>Change Password and Skip for Now have exact semantics/copy; failures keep dialog open and announce unchanged state; successful skip makes no password-change claim; no secret is rendered outside masked inputs.</acceptance_criteria>
  <verify><automated>npm run typecheck && npm run build && npm run test:integration -- tests/integration/credential-setup.test.ts</automated><human-check>At phase UAT, exercise blank/loading/error/change/skip/later-reset at 320px and desktop, keyboard-only, reduced motion, light/dark, and long validation text.</human-check></verify>
  <done>Users complete or skip the one-time prompt safely before reaching the requested business page.</done>
</task>
</tasks>

<threat_model>
**OWASP ASVS 5.0 L1 mapping:** V6.2.2, V6.2.3, and V6.2.6 apply to user password change/current-password verification/masking; D-15 intentionally deviates from V6.4.1 by permitting skip, with the accepted risk documented and reset/revocation mitigations tested; V7.2.1 and V7.4.1 apply to backend session validation/termination. Other controls are N/A. High/critical threats are mitigated; the V6.4.1 deviation is explicitly accepted per locked D-15.
## Trust Boundaries
| Boundary | Description |
|---|---|
| sign-in client→credential API | Passwords and callback URL are untrusted sensitive input. |
| credential API→Better Auth | Credential verification/change must preserve session policy. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-01 | Elevation | cross-branch scope | high | mitigate | Callback does not alter persisted scope; 01-07 remains authority. |
| T-01-02 | Elevation | direct user APIs | high | mitigate | Credential route is current-user only; admin lifecycle remains 01-09. |
| T-01-03 | Elevation | retained sessions | high | mitigate | Active-session checks and reset/change revocation tests. |
| T-01-04 | Tampering | workbook poisoning | high | mitigate | No workbook path here. |
| T-01-05 | Tampering/DoS | production reset | critical | mitigate | No reset surface. |
| T-01-06 | Elevation | second Admin | high | mitigate | Credential API cannot alter role. |
| T-01-07 | Elevation | Better Auth overexposure | high | mitigate | Only narrow change/skip API; no generic admin endpoint/client. |
| T-01-08 | Disclosure | temporary password | high | mitigate | Masking, non-echo, state clearing, no URL/log/cache, offline handoff only. |
| T-01-SC | Tampering | dependencies | high | mitigate | Existing primitives and pinned Better Auth only. |
</threat_model>

## Artifacts this phase produces
- Symbols: credential action schemas/service methods, `CredentialSetupDialog`.
- Route: GET/POST `/api/credential-setup`.
- Fields: uses `credentialSetupRequired`; no new schema fields/CLI flags.
- Files: route, integration test, dialog, modified sign-in/service.

<verification>Credential integration suite, typecheck/build, and end-of-phase interaction/accessibility checks pass.</verification>
<success_criteria>Prompt timing and consumption match D-15 exactly and no password value crosses an unauthorized presentation/storage boundary.</success_criteria>
<output>Create `.planning/phases/01-trusted-foundation-and-data-onboarding/01-10-SUMMARY.md` when done.</output>
