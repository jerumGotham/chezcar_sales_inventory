---
phase: 01-trusted-foundation-and-data-onboarding
plan: 11
type: execute
wave: 10
depends_on: [01-08, 01-09, 01-10]
files_modified:
  - app/users/page.tsx
  - app/users/users-client.tsx
autonomous: true
requirements: [REQ-user-management, REQ-role-authorization]
estimate: { tokens: 58000, raw_tokens: 58000, tasks: 2, confidence: low }
must_haves:
  truths:
    - "Empty: unfiltered shows `No staff users yet` plus Create User; filtered-empty shows separate copy plus Reset Filters; credential fields start blank."
    - "Loading: forbidden navigation never flashes; first load says `Loading users…`; refetch retains rows with `Updating…`; mutations prevent duplicate submit with progress labels."
    - "Error: session/access outcomes fail safely; load and form errors use exact copy, retain input, focus/announce problems, and never claim success early."
    - "Populated: seven columns, human role/scope/status labels, lifecycle actions, result count, and ten-row pagination render from API data."
    - "Partial: missing last sign-in says `Never`; invalid assignment cannot submit; role changes clear incompatible location; rejected forms retain non-secret values."
    - "Overflow: sidebar/denial/table/dialog behavior follows UI-SPEC; names/emails wrap and identifiers retain accessible full values."
    - "Zero-one-many: zero avoids `0 to 0 of 0`, one says `1 user`, many says `{n} users`, with valid ranges/pagination."
    - "Long-text: human/error/action text wraps/reflows; action labels do not truncate; identifiers ellipsize only with accessible full value; touch targets stay at least 44px."
  artifacts:
    - { path: "app/users/page.tsx", provides: "Admin-gated server page" }
    - { path: "app/users/users-client.tsx", provides: "approved filtered table and account lifecycle UI" }
  key_links:
    - { from: "users-client.tsx", to: "/api/users", via: "typed same-origin query/mutations" }
    - { from: "mutation success", to: "complete applied React Query key", via: "invalidate/refetch preserving valid page/filters" }
  prohibitions:
    - statement: "User Management must not restore prototype mock users, editable permissions/custom roles, Delete language, stale role names, react-select styling, or console/local-only submissions."
      status: unverified
      flagged: true
---

<objective>Replace the mock Users & Roles prototype with the approved, accessible Admin User Management experience backed by durable APIs.</objective>
<execution_context>@C:/Users/Jerum/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Jerum/.config/opencode/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/01-trusted-foundation-and-data-onboarding/01-CONTEXT.md
@.planning/phases/01-trusted-foundation-and-data-onboarding/01-UI-SPEC.md
@.planning/phases/01-trusted-foundation-and-data-onboarding/01-PATTERNS.md
@.planning/phases/01-trusted-foundation-and-data-onboarding/01-08-SUMMARY.md
@.planning/phases/01-trusted-foundation-and-data-onboarding/01-09-SUMMARY.md
@.planning/phases/01-trusted-foundation-and-data-onboarding/01-10-SUMMARY.md
@app/users/page.tsx
@app/products/page.tsx
@lib/contracts/users.ts</context>

<tasks>
<task type="auto">
  <name>Task 1: Render the durable Admin staff list and all table states</name>
  <read_first>D-10-D-14/D-18; complete UI-SPEC Design System/Copy/Role-aware shell/Admin User Management/UI Considerations; current users page; products page query analog; user contract</read_first>
  <files>app/users/page.tsx, app/users/users-client.tsx</files>
  <action>Make `/users` an Admin-gated server page and move interactive state into `UsersClient`. Use PageShell, exact title/subtitle/Create User, three staff-only summary cards, Base UI Select filters, complete query key (page/pageSize/search/role/location/status), applied/reset semantics, retained rows while fetching, seven-column semantic table, owner row with Owner Admin/no actions, and 10-row pagination. Implement all eight must_haves UI considerations exactly, D-10/D-14/D-18, semantic tokens, light/dark, mobile-first overflow, and no protected flash.</action>
  <acceptance_criteria>List data comes only from typed API; owner is visible but excluded from staff counts and immutable; all exact empty/loading/error/populated/partial/overflow/zero-one-many/long-text assertions are represented in render branches.</acceptance_criteria>
  <verify><automated>npm run typecheck && npm run build</automated><human-check>At phase UAT, cover first-load, update, both empty states, error/retry, one/many rows, pagination, long names/emails/errors, 320px/desktop, and light/dark.</human-check></verify>
  <done>The Admin can browse durable staff data with every approved state and no stale permission UI.</done>
</task>

<task type="auto">
  <name>Task 2: Wire exact create/edit/deactivate/reactivate/reset dialogs</name>
  <read_first>D-13-D-18; UI-SPEC create/edit/lifecycle/copy/accessibility sections; UsersClient list state; user contract mutation functions; existing Dialog primitives</read_first>
  <files>app/users/users-client.tsx</files>
  <action>Implement create fields/order/defaults, role-dependent read-only/branch scope, incompatible-location clearing, masked temporary password confirmation/offline helper, edit without credential fields, revocation warning, separate reset dialog, destructive deactivate and neutral reactivate confirmations, exact footer/progress/failure/success copy, field blur/submit validation, first-invalid focus, aria states, retained values, query invalidation, and valid filter/page preservation. Role options are exactly Stock/Branch/Accounting; use Deactivate not Delete. Owner controls remain absent.</action>
  <acceptance_criteria>Each operation calls its narrow API once while in flight, success closes/refetches/announces exact copy, failure remains open with unchanged-state copy and values, and invalid role/location/password data never submits.</acceptance_criteria>
  <verify><automated>npm run typecheck && npm run build && npm run test:integration -- tests/integration/user-management.test.ts tests/integration/session-revocation.test.ts</automated><human-check>At phase UAT, exercise create/edit/reset/deactivate/reactivate success/failure, keyboard/focus/Escape, 320px scrolling, long text, light/dark, and verify owner/no-Admin controls.</human-check></verify>
  <done>Admin User Management performs every durable non-Admin lifecycle action with exact safe feedback.</done>
</task>
</tasks>

<threat_model>
**OWASP ASVS 5.0 L1 mapping:** V2.2.2 applies because client validation remains usability-only and server services validate; V8.2.1 and V8.3.1 apply to Admin-only functions at the trusted service; V15.3.1 applies to rendering/caching only the required DTO fields. Other controls are N/A to this UI slice. High/critical items below are mitigated, not accepted.
## Trust Boundaries
| Boundary | Description |
|---|---|
| API DTO→client UI | Only browser-safe account fields may render/cache. |
| form→user APIs | Role/location/password/target input is untrusted; server remains authority. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-01 | Elevation | cross-branch assignment UI | high | mitigate | Fixed options/clearing plus server validation. |
| T-01-02 | Elevation | direct non-Admin APIs | high | mitigate | UI hidden for roles, but 01-09 server guard is decisive. |
| T-01-03 | Elevation | retained sessions | high | mitigate | UI states immediate sign-out consequence; 01-09 transaction proves it. |
| T-01-04 | Tampering | workbook poisoning | high | mitigate | Location options derive from canonical safe DTOs. |
| T-01-05 | Tampering/DoS | production reset | critical | mitigate | No workbook/reset UI or API. |
| T-01-06 | Elevation | second Admin | high | mitigate | No Admin option/action; owner row immutable; server/DB enforcement. |
| T-01-07 | Elevation | Better Auth overexposure | high | mitigate | Client calls only Chezcar typed routes. |
| T-01-08 | Disclosure | temporary password | high | mitigate | Masked local form only, cleared on success, never rendered in banners/cache. |
| T-01-SC | Tampering | UI registry | high | mitigate | Existing approved shadcn/Base UI primitives only. |
</threat_model>

## Artifacts this phase produces
- Symbol: `UsersClient` and colocated typed UI state helpers.
- Files: rewritten server `app/users/page.tsx`; new `app/users/users-client.tsx`.
- No CLI flags, schema fields, routes, or packages.

<verification>Typecheck/build, backend lifecycle suites, and all eight explicit UI-state human checks complete; lint is reported separately as the known baseline.</verification>
<success_criteria>The approved User Management contract is durable, responsive, accessible, fixed-role, and owner-safe.</success_criteria>
<output>Create `.planning/phases/01-trusted-foundation-and-data-onboarding/01-11-SUMMARY.md` when done.</output>
