---
phase: 01-trusted-foundation-and-data-onboarding
plan: 03
type: execute
wave: 3
depends_on: [01-02]
files_modified:
  - scripts/data-onboarding/workbook-profile.mjs
  - scripts/data-onboarding/workbook-profile.test.ts
  - scripts/data-onboarding/canonicalize.mjs
  - scripts/data-onboarding/canonicalize.d.mts
  - scripts/data-onboarding/canonicalize.test.ts
  - scripts/data-onboarding/source-mapping.json
  - scripts/data-onboarding/review-report.json
  - scripts/data-onboarding/resolutions.json
autonomous: true
requirements: [REQ-data-onboarding]
estimate: { tokens: 42000, raw_tokens: 42000, tasks: 2, confidence: low }
must_haves:
  truths:
    - "The complete workbook is profiled read-only with source traceability per D-01/D-02."
    - "SR/BL BEFORE ambiguity, duplicate collisions, negative/blank/nonnumeric quantities, and missing/nonnumeric/conflicting prices are blocking findings per D-03 and D-06-D-09."
    - "Empty or unresolved input cannot produce a partial canonical seed."
  artifacts:
    - { path: "scripts/data-onboarding/review-report.json", provides: "owner-reviewable blocking findings" }
    - { path: "scripts/data-onboarding/source-mapping.json", provides: "source-to-canonical traceability" }
  key_links:
    - { from: "workbook-profile.mjs", to: "canonicalize.mjs", via: "declared profile evidence contract" }
    - { from: "review-report.json", to: "01-04 owner checkpoint", via: "all blocking findings" }
  prohibitions:
    - statement: "The tooling must not auto-merge suspected duplicates, coerce invalid quantities, select a price winner, or infer SR/BL BEFORE meaning."
      status: unverified
      flagged: true
---

<objective>Produce the complete owner-review evidence package, without canonical fixture generation.</objective>
<execution_context>@C:/Users/Jerum/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Jerum/.config/opencode/gsd-core/templates/summary.md</execution_context>
<context>@.planning/PROJECT.md
@.planning/phases/01-trusted-foundation-and-data-onboarding/01-CONTEXT.md
@.planning/phases/01-trusted-foundation-and-data-onboarding/01-RESEARCH.md
@.planning/phases/01-trusted-foundation-and-data-onboarding/01-PATTERNS.md
@.planning/phases/01-trusted-foundation-and-data-onboarding/01-02-SUMMARY.md
@scripts/data-onboarding/workbook-profile.mjs</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Classify rows and fail closed on every blocking source decision</name>
  <read_first>01-CONTEXT.md D-01-D-09; 01-RESEARCH.md Workbook Evidence/Pitfalls/Open Questions execution gate; 01-PATTERNS.md Data-onboarding patterns; workbook-profile.mjs/declarations/test</read_first>
  <files>scripts/data-onboarding/canonicalize.mjs, scripts/data-onboarding/canonicalize.d.mts, scripts/data-onboarding/canonicalize.test.ts</files>
  <behavior>- Heading/spacer rows never become products.
- Product rows retain source sheet/row/column/raw/normalized/formula/cache evidence.
- Duplicate/suspected duplicate, invalid quantity, unresolved location source, missing/nonnumeric/conflicting price produce blocking records.
- No output records are returned while any blocker is unresolved.</behavior>
  <action>Define Zod contracts plus strict `.d.mts` exports for `ProfileEvidence`, `ReviewFinding`, `ResolutionRecord`, and `CanonicalCandidate`; implement the `// @ts-check` executable-compatible module with pure `classifySourceRow` and `buildReviewFindings`. Generate temporary codes only after row classification, using the plan-05 format. Encode D-06/D-08/D-09 as blocking outcomes and D-03 as fixed canonical target codes whose workbook sources remain explicitly unresolved until Plan 01-04.</action>
  <acceptance_criteria>Table-driven tests cover every malformed input and assert zero canonical records whenever one blocker remains; source coordinates/raw and normalized evidence survive classification.</acceptance_criteria>
  <verify><automated>npm run test -- scripts/data-onboarding/canonicalize.test.ts</automated></verify>
  <done>Canonicalization is impossible until all owner-dependent findings have explicit resolutions.</done>
</task>

<task type="auto">
  <name>Task 2: Profile the owner workbook and emit the review package</name>
  <read_first>excel/REALTIME INVENTORY- NEW 3.xlsx (read-only); 01-RESEARCH.md Open Questions execution gate; workbook-profile.mjs; canonicalize.mjs/declarations/test</read_first>
  <files>scripts/data-onboarding/workbook-profile.mjs, scripts/data-onboarding/workbook-profile.test.ts, scripts/data-onboarding/source-mapping.json, scripts/data-onboarding/review-report.json, scripts/data-onboarding/resolutions.json</files>
  <action>Expand the Node 20 `.mjs` CLI with `--workbook`, repeatable `--sheet`, `--mapping-out`, `--report-out`, and `--resolutions-out`, using the `data:profile` package script established in Plan 01-02 as the only supported invocation. Run that package script against the owner workbook to capture workbook hash, selected sources, formulas/caches, totals, row classifications, and all blockers. Emit a deterministic blank `resolutions.json` template keyed by finding ID. Keep the workbook path as input only; no canonical fixture is produced in this plan.</action>
  <acceptance_criteria>The report explicitly contains SR source and BL BEFORE decisions, each duplicate code collision, every invalid quantity, and each missing/nonnumeric/conflicting price; each record is traceable to source coordinates and an unresolved resolution key.</acceptance_criteria>
  <verify><automated>npm run test -- scripts/data-onboarding/workbook-profile.test.ts scripts/data-onboarding/canonicalize.test.ts && npm run data:profile -- --workbook "excel/REALTIME INVENTORY- NEW 3.xlsx" --mapping-out scripts/data-onboarding/source-mapping.json --report-out scripts/data-onboarding/review-report.json --resolutions-out scripts/data-onboarding/resolutions.json</automated></verify>
  <done>The owner has one stable, complete review package and no approved canonical fixture exists yet.</done>
</task>
</tasks>

<threat_model>
**OWASP ASVS 5.0 L1 mapping:** V2.2.1 applies to positive workbook/finding validation, V2.2.2 to enforcing it in the trusted CLI boundary, and V2.3.1 to the profile-before-review sequence. Authentication/authorization controls are N/A to this evidence-only plan. High/critical items below are mitigated, not accepted.
## Trust Boundaries
| Boundary | Description |
|---|---|
| workbook→review artifacts | Untrusted formulas, references, and values become evidence only. |
| review artifacts→future fixture | Unresolved decisions must stop output. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-01 | Elevation | cross-branch scope | high | mitigate | Deferred to persisted-policy plan 01-07. |
| T-01-02 | Elevation | direct non-Admin user APIs | high | mitigate | Deferred to 01-09 narrow routes. |
| T-01-03 | Elevation | retained sessions | high | mitigate | Deferred to 01-09 atomic revocation. |
| T-01-04 | Tampering | formula/reference poisoning | high | mitigate | Preserve formula/cache/reference evidence; execute none; block disagreements. |
| T-01-05 | Tampering/DoS | production reset | critical | mitigate | No database/reset code in this plan; 01-06 adds hard refusal. |
| T-01-06 | Elevation | second Admin | high | mitigate | 01-06/01-09 enforce singleton owner. |
| T-01-07 | Elevation | Better Auth overexposure | high | mitigate | No auth surface here; 01-09 uses narrow APIs. |
| T-01-08 | Information Disclosure | password leakage | high | mitigate | Review artifacts contain no credentials. |
| T-01-SC | Tampering | package tree | high | mitigate | Uses only 01-01-approved locked dependencies. |
</threat_model>

## Artifacts this phase produces
- Symbols: `ProfileEvidence`, `ReviewFinding`, `ResolutionRecord`, `CanonicalCandidate`, `classifySourceRow`, `buildReviewFindings`.
- CLI flags: `--workbook`, `--sheet`, `--mapping-out`, `--report-out`, `--resolutions-out`.
- Files: `canonicalize.mjs`, `canonicalize.d.mts`, its TypeScript test, `source-mapping.json`, `review-report.json`, and `resolutions.json`.
- Fields: finding IDs, source coordinates/raw/normalized/formula/cache values, blocking status, owner resolution.

<verification>Both focused suites pass and the real-workbook command exits successfully while leaving all owner decisions unresolved.</verification>
<success_criteria>No blocking item is absent from the report and no canonical fixture is generated.</success_criteria>
<output>Create `.planning/phases/01-trusted-foundation-and-data-onboarding/01-03-SUMMARY.md` when done.</output>
