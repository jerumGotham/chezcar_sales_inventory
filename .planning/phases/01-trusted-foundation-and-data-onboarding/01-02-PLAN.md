---
phase: 01-trusted-foundation-and-data-onboarding
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - package.json
  - package-lock.json
  - vitest.config.ts
  - scripts/data-onboarding/workbook-profile.ts
  - scripts/data-onboarding/workbook-profile.test.ts
  - tests/helpers/database.ts
  - tests/helpers/database.test.ts
  - tests/helpers/factories.ts
  - tests/helpers/requests.ts
  - tests/fixtures/create-workbook-fixture.ts
  - tests/fixtures/workbook-edge-cases.xlsx
autonomous: true
requirements: [REQ-data-onboarding, REQ-role-authorization, REQ-user-management]
estimate: { tokens: 48000, raw_tokens: 48000, tasks: 3, confidence: low }
must_haves:
  truths:
    - "A developer can run one read-only owner-workbook profile path end-to-end without database writes (D-01, D-02)."
    - "Formula cells are inspected as source evidence and never executed."
    - "Unit and disposable-PostgreSQL verification commands exist before later tasks rely on them."
  artifacts:
    - { path: "vitest.config.ts", provides: "Node test projects" }
    - { path: "scripts/data-onboarding/workbook-profile.ts", provides: "read-only XLSX profile CLI" }
    - { path: "tests/helpers/database.ts", provides: "protected disposable PostgreSQL harness" }
  key_links:
    - { from: "workbook-profile.ts", to: "excel/REALTIME INVENTORY- NEW 3.xlsx", via: "SheetJS selected-sheet read-only parse" }
    - { from: "vitest.config.ts", to: "tests/helpers/database.ts", via: "serial integration project" }
  prohibitions:
    - statement: "Workbook onboarding must not become an Admin upload/import screen, browser file picker, or HTTP import/reset endpoint."
      status: unverified
      flagged: true
---

<objective>
Deliver the production-quality tracer: a tested developer CLI reads the owner workbook and emits a safe profile, while Phase 1 gains deterministic test infrastructure.

Purpose: Prove the unfamiliar XLSX boundary and verification architecture before canonicalization, schema, API, or UI expansion.
Output: Approved dependencies, Vitest projects, workbook profiler, synthetic workbook, and protected database/request helpers.
</objective>

<execution_context>@C:/Users/Jerum/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Jerum/.config/opencode/gsd-core/templates/summary.md</execution_context>
<context>
@.planning/PROJECT.md
@.planning/phases/01-trusted-foundation-and-data-onboarding/01-CONTEXT.md
@.planning/phases/01-trusted-foundation-and-data-onboarding/01-RESEARCH.md
@.planning/phases/01-trusted-foundation-and-data-onboarding/01-PATTERNS.md
@.planning/phases/01-trusted-foundation-and-data-onboarding/01-VALIDATION.md
@package.json
@prisma/seed.mjs
</context>

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: End-to-end read-only workbook profile — one selected sheet</name>
  <read_first>01-CONTEXT.md D-01/D-02/D-06-D-09; 01-RESEARCH.md Standard Stack, Workbook Evidence, Package Legitimacy Audit; 01-PATTERNS.md Data-onboarding tooling; package.json; prisma/seed.mjs</read_first>
  <files>package.json, package-lock.json, vitest.config.ts, scripts/data-onboarding/workbook-profile.ts, scripts/data-onboarding/workbook-profile.test.ts</files>
  <behavior>- A selected sheet yields workbook SHA-256, sheet visibility/name, dimensions, source coordinates, raw values, formulas, and cached values.
- Missing/empty workbook input exits nonzero and writes no canonical fixture.
- Formula text is data only; external links/macros and formula/cache disagreements become findings.
- The owner XLSX is never modified.</behavior>
  <action>Install only the human-approved Vitest version and official SheetJS CE 0.20.3 CDN tarball; add one-shot `test` and serial `test:integration` scripts and Node projects. Implement `profileWorkbook` plus CLI entry in `workbook-profile.ts` using SheetJS selected-sheet parsing, file hashing, explicit row/cell source coordinates, and JSON stdout. Start with failing tests, then make the single profile path pass. Do not import Prisma or write output beside the input workbook. Implement D-01/D-02 and preserve D-06-D-09 findings without cleanup.</action>
  <acceptance_criteria>`npm run test -- scripts/data-onboarding/workbook-profile.test.ts` proves source traceability, formula non-execution, missing/empty refusal, and read-only behavior; package-lock records exact approved versions.</acceptance_criteria>
  <verify><automated>npm run test -- scripts/data-onboarding/workbook-profile.test.ts</automated></verify>
  <done>One owner-workbook sheet can be profiled end-to-end with real source evidence and no database mutation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Protect disposable PostgreSQL and direct-request tests</name>
  <read_first>01-VALIDATION.md Wave 0 Requirements; docker-compose.yml; lib/server/prisma.ts; prisma/schema.prisma; app/api/inventory/route.ts</read_first>
  <files>tests/helpers/database.ts, tests/helpers/database.test.ts, tests/helpers/factories.ts, tests/helpers/requests.ts</files>
  <behavior>- Harness accepts only a separately named test database/container target.
- Development bind-mount and unknown URLs fail before destructive commands.
- Factories create canonical fixed-role/location users and sessions, not prototype fixtures.</behavior>
  <action>Create Node helpers for lifecycle of a separately named PostgreSQL 17 container/database, serial migration execution, cleanup, canonical location/user/session factories, and Request construction. Export `assertDisposableDatabaseUrl`, `withDisposableDatabase`, `createAuthFixture`, and `createRequest`. Positively reject the checked-in development database name/port and any target lacking the test marker; never inspect or reset `data/`.</action>
  <acceptance_criteria>Helper unit checks reject the Compose development target and an arbitrary URL, accept the dedicated test target, and TypeScript compiles without importing page mock data.</acceptance_criteria>
  <verify><automated>npm run test -- tests/helpers/database.test.ts && npm run typecheck</automated></verify>
  <done>Later integration tests cannot target the known development bind mount or an unrecognized database.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create the deterministic hostile workbook fixture</name>
  <read_first>01-RESEARCH.md Wave 0 Gaps and Common Pitfalls; workbook-profile.ts; workbook-profile.test.ts</read_first>
  <files>tests/fixtures/create-workbook-fixture.ts, tests/fixtures/workbook-edge-cases.xlsx, scripts/data-onboarding/workbook-profile.test.ts</files>
  <behavior>- Fixture contains hidden sheet, category/spacer rows, formulas/caches, duplicate and missing codes, negative/blank/nonnumeric quantities, and missing/conflicting/nonnumeric prices.
- Rebuilding the fixture yields equivalent parsed evidence.</behavior>
  <action>Add a deterministic fixture builder and check in its small XLSX output. Expand profiler tests against every required edge; keep the 116MB owner workbook out of quick tests. The fixture builder is test support only and never reads or rewrites the owner workbook.</action>
  <acceptance_criteria>The checked-in fixture is small, reproducible, and every anomaly named by 01-VALIDATION Wave 0 is asserted by the profiler test.</acceptance_criteria>
  <verify><automated>npm run test -- scripts/data-onboarding/workbook-profile.test.ts</automated></verify>
  <done>Quick tests reproduce workbook poisoning and ambiguity cases without depending on the owner binary.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| workbook→developer CLI | Formula/reference content and oversized workbook data are untrusted. |
| test harness→PostgreSQL | Destructive setup must target only a disposable database. |
| browser→server | Later tests model hostile role/location parameters. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-01 | Elevation | cross-branch scope manipulation | high | mitigate | Request factories support hostile-scope tests in 01-07. |
| T-01-02 | Elevation | direct non-Admin user APIs | high | mitigate | Auth fixtures support direct API tests in 01-09. |
| T-01-03 | Elevation | retained sessions | high | mitigate | Session factories support old-cookie tests in 01-09. |
| T-01-04 | Tampering | formula/reference poisoning | high | mitigate | Formula non-execution, cached/reference findings, selected-sheet parsing. |
| T-01-05 | Tampering/DoS | production reset | critical | mitigate | Disposable-target assertion before destructive test setup. |
| T-01-06 | Elevation | second Admin | high | mitigate | Fixed-role factories and later DB/service guards. |
| T-01-07 | Elevation | Better Auth endpoint overexposure | high | mitigate | Tests use narrow request factories; 01-09 closes routes. |
| T-01-08 | Information Disclosure | temporary password | high | mitigate | Fixtures do not log credentials; 01-09/10 test non-disclosure. |
| T-01-SC | Tampering | npm/CDN installs | high | mitigate | 01-01 approval plus exact lockfile versions. |
</threat_model>

## Artifacts this phase produces
- Symbols: `profileWorkbook`, `assertDisposableDatabaseUrl`, `withDisposableDatabase`, `createAuthFixture`, `createRequest`.
- CLI flags: initial profiler input path/selected-sheet flags documented by `--help`.
- Files: every path in `files_modified`; no schema fields in this plan.

<verification>`npm run test -- scripts/data-onboarding/workbook-profile.test.ts`, helper tests, and `npm run typecheck` pass under Node 20.</verification>
<success_criteria>The workbook tracer is real, read-only, deterministic under test, and later test suites have safe infrastructure.</success_criteria>
<output>Create `.planning/phases/01-trusted-foundation-and-data-onboarding/01-02-SUMMARY.md` when done.</output>
