---
phase: 01-trusted-foundation-and-data-onboarding
plan: 08
type: execute
wave: 8
depends_on: [01-07]
files_modified:
  - proxy.ts
  - proxy.test.ts
  - app/access-denied/page.tsx
  - lib/menu.ts
  - components/app-sidebar.tsx
  - components/app-layout-shell.tsx
  - components/location-scope-control.tsx
  - app/inventory/page.tsx
autonomous: true
requirements: [REQ-role-authorization]
estimate: { tokens: 50000, raw_tokens: 50000, tasks: 3, confidence: low }
must_haves:
  truths:
    - "Unauthenticated/inactive/revoked page requests go to sign-in; authenticated forbidden requests show dedicated access denied without protected content (D-11)."
    - "Forbidden navigation never flashes; User Management is ADMIN-only presentation and menu hiding never substitutes for server authorization (D-10)."
    - "Admin sees All or one canonical location; Stock Staff sees read-only SR; Branch Staff its persisted branch; Accounting Business-wide with no assignment (D-12)."
  artifacts:
    - { path: "app/access-denied/page.tsx", provides: "safe denial screen" }
    - { path: "components/location-scope-control.tsx", provides: "server-derived scope feedback" }
  key_links:
    - { from: "proxy.ts", to: "lib/server/policy/access.ts", via: "page capability decision" }
    - { from: "AppLayoutShell", to: "AppSidebar", via: "already-filtered server-derived menu model" }
  prohibitions:
    - statement: "Denied pages must not render or encode protected headers, filters, rows, record identifiers, requested parameters, or retry URLs."
      status: unverified
      flagged: true
---

<objective>Reflect authoritative access safely in page routing, navigation, denial, and location-scope feedback.</objective>
<execution_context>@C:/Users/Jerum/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Jerum/.config/opencode/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/01-trusted-foundation-and-data-onboarding/01-CONTEXT.md
@.planning/phases/01-trusted-foundation-and-data-onboarding/01-UI-SPEC.md
@.planning/phases/01-trusted-foundation-and-data-onboarding/01-PATTERNS.md
@.planning/phases/01-trusted-foundation-and-data-onboarding/01-07-SUMMARY.md
@proxy.ts
@lib/menu.ts
@components/app-sidebar.tsx
@components/app-layout-shell.tsx</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Route authenticated denials without protected data</name>
  <read_first>D-10/D-11; UI-SPEC access-denied surface/copy; proxy.ts; access policy; app/sign-in/page.tsx safe callback analog</read_first>
  <files>proxy.ts, proxy.test.ts, app/access-denied/page.tsx</files>
  <behavior>- Missing/expired/inactive/revoked session redirects to `/sign-in` with safe local callback.
- Authenticated forbidden page redirects to `/access-denied` with no protected query data.
- Denial page renders fixed copy and only `/dashboard` CTA inside authenticated shell.</behavior>
  <action>Replace hard-coded proxy clauses with page capabilities from the central policy and persisted active User/Location context. Build the server-component denial Card using ShieldX and exact UI-SPEC copy. Do not accept denied-resource data. Add route tests for direct forbidden pages, no protected disclosure, safe callbacks, and policy-order invariance.</action>
  <acceptance_criteria>proxy.test.ts proves 401/sign-in versus 403/access-denied distinction and the denial URL/body contain no requested record/filter values.</acceptance_criteria>
  <verify><automated>npm run test -- proxy.test.ts</automated><human-check>At phase UAT, direct forbidden navigation shows only the approved denial card and Back to Dashboard.</human-check></verify>
  <done>Direct page access fails with the correct safe experience.</done>
</task>

<task type="auto">
  <name>Task 2: Render only server-derived permitted navigation</name>
  <read_first>D-10/D-14/D-18; UI-SPEC role-aware shell; menu.ts; app-sidebar.tsx; app-layout-shell.tsx; access policy exports</read_first>
  <files>lib/menu.ts, components/app-sidebar.tsx, components/app-layout-shell.tsx</files>
  <action>Rename Users & Roles to User Management, remove custom-role navigation, attach capability IDs to menu metadata, and pass an already-filtered menu model from the authenticated shell boundary to AppSidebar. Preserve desktop pin/mobile drawer/active-link/accessibility behavior. Never initially render all entries or fetch capabilities in a client effect; server checks remain independent.</action>
  <acceptance_criteria>ADMIN receives User Management; the other three roles do not; no Manage Roles path is rendered; existing permitted links retain active and mobile behavior.</acceptance_criteria>
  <verify><automated>npm run typecheck && npm run test -- proxy.test.ts</automated><human-check>At phase UAT, switch roles and confirm no forbidden-link flash on desktop/mobile.</human-check></verify>
  <done>Navigation accurately reflects persisted capabilities without becoming authorization.</done>
</task>

<task type="auto">
  <name>Task 3: Show canonical, role-fixed location scope</name>
  <read_first>D-12/D-13/D-18; UI-SPEC scope feedback; app/inventory/page.tsx; catalog DTO/query; access context</read_first>
  <files>components/location-scope-control.tsx, app/inventory/page.tsx</files>
  <action>Create `LocationScopeControl` consuming a browser-safe server-derived scope model. Admin gets enabled All locations/SR/QC/BL/LU/VC/SP choices; Stock Staff static Stock Room (SR); Branch Staff static assigned active branch; Accounting static Business-wide with no location ID. Integrate on the location-sensitive inventory surface while server scope remains authoritative. Preserve existing table/filter behavior and D-18 visual direction.</action>
  <acceptance_criteria>Each role renders exactly the UI-SPEC scope label/options and no fixed-scope role can submit a broader location.</acceptance_criteria>
  <verify><automated>npm run typecheck && npm run build</automated><human-check>At phase UAT, inspect all four role scope controls in light/dark and 320px/desktop layouts.</human-check></verify>
  <done>Users can see their authoritative location scope without gaining a client-side bypass.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| page request→proxy | URL and session are untrusted before persisted policy. |
| server capability model→client shell | Only permitted presentation data may hydrate. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-01 | Disclosure | cross-branch UI scope | high | mitigate | Persisted scope model and fixed controls; API remains authoritative. |
| T-01-02 | Elevation | direct non-Admin user APIs | high | mitigate | Menu is UX only; 01-09 routes require Admin. |
| T-01-03 | Elevation | retained sessions | high | mitigate | Proxy reloads active user; 01-09 deletes sessions. |
| T-01-04 | Tampering | workbook poisoning | high | mitigate | UI reads canonical persisted data only. |
| T-01-05 | Tampering/DoS | production reset | critical | mitigate | No UI/HTTP reset surface. |
| T-01-06 | Elevation | second Admin | high | mitigate | No Admin creation option; DB/service controls remain. |
| T-01-07 | Elevation | Better Auth overexposure | high | mitigate | Proxy uses session read only; no generic Admin client. |
| T-01-08 | Disclosure | temporary password | high | mitigate | Shell/denial/scope models exclude credentials. |
| T-01-SC | Tampering | dependencies | high | mitigate | Existing UI primitives only. |
</threat_model>

## Artifacts this phase produces
- Symbols: `LocationScopeControl`, filtered menu/capability model props.
- Route/file: `/access-denied`, `app/access-denied/page.tsx`, `proxy.test.ts`.
- No schema fields or CLI flags.

<verification>Proxy tests, typecheck, build, and end-of-phase shell/denial/scope human checks complete.</verification>
<success_criteria>Page routing and shell presentation fail closed, disclose no protected data, and match D-10-D-13/D-18.</success_criteria>
<output>Create `.planning/phases/01-trusted-foundation-and-data-onboarding/01-08-SUMMARY.md` when done.</output>
