---
phase: 01-trusted-foundation-and-data-onboarding
plan: 08
type: execute
wave: 8
depends_on: [01-07]
files_modified:
  - lib/contracts/access.ts
  - lib/server/shell.ts
  - lib/server/shell.test.ts
  - lib/menu.ts
  - components/app-sidebar.tsx
  - components/app-layout-shell.tsx
  - components/app-layout-shell-client.tsx
  - components/shell-access-context.tsx
  - components/app-header.tsx
autonomous: true
requirements: [REQ-role-authorization]
estimate: { tokens: 44000, raw_tokens: 44000, tasks: 3, confidence: low }
must_haves:
  truths:
    - "Forbidden navigation never flashes; User Management is ADMIN-only presentation and menu hiding never substitutes for server authorization (D-10)."
    - "The authenticated global AppHeader renders authoritative persisted scope feedback for every role: Admin `All locations` or the selected canonical location, Stock Staff `Stock Room (SR)`, Branch Staff its assigned active branch, and Accounting Staff `Business-wide` with no location assignment (D-12/D-13)."
    - "Accounting Staff is not granted inventory capability; its `Business-wide` feedback is proven in the global AppHeader, never through Inventory page controls."
  artifacts:
    - { path: "lib/contracts/access.ts", provides: "browser-safe persisted shell and location-scope DTOs" }
    - { path: "lib/server/shell.ts", provides: "server-only authenticated shell/page loader" }
    - { path: "lib/server/shell.test.ts", provides: "four-role scope/menu contract proof" }
    - { path: "components/app-header.tsx", provides: "globally accessible persisted scope feedback" }
  key_links:
    - { from: "lib/server/shell.ts", to: "ShellAccessProvider", via: "browser-safe ShellAccessDto loaded from persisted User/Location" }
    - { from: "ShellAccessProvider", to: "AppHeader", via: "useShellAccess scope DTO on every authenticated PageShell" }
    - { from: "AppLayoutShellClient", to: "AppSidebar", via: "already-filtered menu model" }
  prohibitions:
    - statement: "Accounting's Business-wide label must not be implemented by granting inventory access, rendering Inventory controls, or treating no location assignment as an all-inventory entitlement."
      status: unverified
      flagged: true
---

<objective>Render persisted role/location scope and permitted navigation through a browser-safe authenticated shell without widening Accounting access.</objective>
<execution_context>@C:/Users/Jerum/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Jerum/.config/opencode/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/01-trusted-foundation-and-data-onboarding/01-CONTEXT.md
@.planning/phases/01-trusted-foundation-and-data-onboarding/01-UI-SPEC.md
@.planning/phases/01-trusted-foundation-and-data-onboarding/01-PATTERNS.md
@.planning/phases/01-trusted-foundation-and-data-onboarding/01-07-SUMMARY.md
@lib/menu.ts
@components/app-sidebar.tsx
@components/app-layout-shell.tsx
@components/app-header.tsx
@components/page-shell.tsx</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Derive the four-role browser-safe shell scope contract</name>
  <read_first>D-10/D-12-D-14/D-18; UI-SPEC role-aware shell; app/layout.tsx server-component constraint; menu.ts; app-sidebar.tsx; app-layout-shell.tsx; access policy and persisted context exports</read_first>
  <files>lib/contracts/access.ts, lib/server/shell.ts, lib/server/shell.test.ts</files>
  <behavior>- ADMIN DTO exposes allowed menu/capabilities and `All locations` or one selected canonical location.
- STOCK_STAFF DTO exposes only allowed menu/capabilities and `Stock Room (SR)`.
- BRANCH_STAFF DTO exposes only allowed menu/capabilities and its persisted active branch code/name.
- ACCOUNTING_STAFF DTO exposes allowed Accounting navigation and `Business-wide` with null location ID, while `inventory:view` remains absent.
- Missing/inactive/invalid assignment fails closed; unauthenticated auth-page loading returns anonymous/no-menu data.</behavior>
  <action>Define browser-safe `ShellAccessDto` and `LocationScopeDto` containing only display identity, filtered menu entries, capability IDs, and persisted scope labels/IDs. Add server-only `loadShellAccess(headers)` that reloads active User/Location and derives DTOs from central policy. Encode Accounting `Business-wide` as presentation scope with null location and no inventory entitlement. Add table-driven tests for all four roles, selected Admin location, invalid assignments, anonymous auth page, exact menu filtering, and explicit Accounting inventory-capability denial.</action>
  <acceptance_criteria>Tests prove exact label/location/capability/menu output for all four roles; Accounting returns `Business-wide`, null location, and no inventory capability.</acceptance_criteria>
  <verify><automated>npm run test -- lib/server/shell.test.ts && npm run typecheck</automated></verify>
  <done>One persisted server loader produces the complete browser-safe shell contract without broadening access.</done>
</task>

<task type="auto">
  <name>Task 2: Preserve the server root while providing shell access to client children</name>
  <read_first>app/layout.tsx; current app-layout-shell.tsx/app-sidebar.tsx; ShellAccessDto/loadShellAccess; UI-SPEC no-flash/mobile shell rules</read_first>
  <files>components/app-layout-shell.tsx, components/app-layout-shell-client.tsx, components/shell-access-context.tsx, components/app-sidebar.tsx</files>
  <action>Convert `AppLayoutShell` into an async server boundary consumed by unchanged server `app/layout.tsx`; move only pathname/POS/auth-layout and drawer interactions into `AppLayoutShellClient`. Add `ShellAccessProvider`/`useShellAccess`, pass the persisted DTO into the provider, and pass already-filtered menu entries into `AppSidebar`. The client shell must not import Prisma, auth, or policy, render the full menu before data arrives, or infer Accounting inventory rights from `Business-wide`.</action>
  <acceptance_criteria>`app/layout.tsx` remains a server component; authenticated descendants receive one server-derived DTO; sidebar behavior remains responsive/accessibly filtered with no forbidden-link flash.</acceptance_criteria>
  <verify><automated>npm run test -- lib/server/shell.test.ts && npm run typecheck</automated><human-check>At phase UAT, confirm no forbidden-link flash and preserved desktop/mobile sidebar behavior for all four roles.</human-check></verify>
  <done>The global shell distributes persisted access state without moving authorization into the browser.</done>
</task>

<task type="auto">
  <name>Task 3: Render authoritative scope in the globally accessible AppHeader</name>
  <read_first>D-12/D-13/D-18; UI-SPEC scope labels; menu.ts; app-header.tsx; PageShell; ShellAccessProvider contract</read_first>
  <files>lib/menu.ts, components/app-header.tsx</files>
  <action>Rename Users & Roles to User Management, remove custom-role navigation, and attach capability IDs used by the server loader. Update `AppHeader` to consume `useShellAccess()` and render a persistent, accessible scope label near authenticated identity: Admin `All locations` or selected location, Stock Staff `Stock Room (SR)`, Branch Staff assigned active branch, Accounting Staff `Business-wide`. Keep Accounting's label informational and do not link it to Inventory. Preserve title/subtitle, theme, logout, responsive wrapping, dark mode, and D-18 styling.</action>
  <acceptance_criteria>Server-loader tests prove all four role labels/capabilities; end-of-phase UAT confirms AppHeader displays each exact label on an Accounting-accessible page and Accounting has no Inventory navigation/control/direct capability.</acceptance_criteria>
  <verify><automated>npm run test -- lib/server/shell.test.ts && npm run typecheck && npm run build</automated><human-check>At phase UAT, inspect AppHeader scope feedback for Admin, Stock Staff, Branch Staff, and Accounting on each role's permitted page at 320px/desktop and light/dark.</human-check></verify>
  <done>Every authenticated role sees authoritative global scope feedback, including Accounting `Business-wide`, without an authorization expansion.</done>
</task>
</tasks>

<threat_model>
**OWASP ASVS 5.0 L1 mapping:** V7.2.1 applies to server-side session reload; V8.2.1, V8.2.2, and V8.3.1 apply to page/menu/data-scope decisions at trusted boundaries; V15.3.1 applies to minimal browser-safe DTO fields. Other controls are N/A to this shell/presentation slice. High/critical items below are mitigated, not accepted.
## Trust Boundaries
| Boundary | Description |
|---|---|
| server capability model→client shell | Only permitted presentation data may hydrate. |
| shell context→global AppHeader | Scope labels must reflect persisted assignment without implying additional capabilities. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-01 | Disclosure | cross-branch/global scope feedback | high | mitigate | Persisted DTO and four-role tests; Accounting label carries no inventory capability. |
| T-01-02 | Elevation | direct non-Admin user APIs | high | mitigate | Menu is UX only; 01-09 routes require Admin. |
| T-01-03 | Elevation | retained sessions | high | mitigate | Proxy reloads active user; 01-09 deletes sessions. |
| T-01-04 | Tampering | workbook poisoning | high | mitigate | UI reads canonical persisted data only. |
| T-01-05 | Tampering/DoS | production reset | critical | mitigate | No UI/HTTP reset surface. |
| T-01-06 | Elevation | second Admin | high | mitigate | No Admin creation option; DB/service controls remain. |
| T-01-07 | Elevation | Better Auth overexposure | high | mitigate | Proxy uses session read only; no generic Admin client. |
| T-01-08 | Disclosure | temporary password | high | mitigate | Shell/denial/scope models exclude credentials. |
| T-01-SC | Tampering | dependencies | high | mitigate | Existing UI primitives only. |
</threat_model>

## Artifacts this phase produces
- Symbols: `ShellAccessDto`, `LocationScopeDto`, `loadShellAccess`, `AppLayoutShellClient`, `LocationScopeControl`, `InventoryClient`, filtered menu/capability model props.
- UI wiring: `ShellAccessProvider` passes the persisted DTO to global `AppHeader` and filtered navigation.
- No schema fields or CLI flags.

<verification>Four-role shell tests, typecheck/build, and end-of-phase global header/navigation checks complete.</verification>
<success_criteria>The authenticated shell and AppHeader show exact persisted scope for all roles, Accounting remains inventory-denied, and D-10/D-12/D-13/D-18 are met.</success_criteria>
<output>Create `.planning/phases/01-trusted-foundation-and-data-onboarding/01-08-SUMMARY.md` when done.</output>
