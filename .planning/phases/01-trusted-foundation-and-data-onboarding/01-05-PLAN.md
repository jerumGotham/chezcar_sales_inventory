---
phase: 01-trusted-foundation-and-data-onboarding
plan: 05
type: execute
wave: 5
depends_on: [01-04]
files_modified:
  - scripts/data-onboarding/resolutions.json
  - scripts/data-onboarding/canonicalize.test.ts
  - scripts/data-onboarding/generate-seed.ts
  - scripts/data-onboarding/generate-seed.test.ts
  - scripts/data-onboarding/source-mapping.json
  - prisma/fixtures/opening-catalog.json
autonomous: true
requirements: [REQ-data-onboarding]
estimate: { tokens: 42000, raw_tokens: 42000, tasks: 2, confidence: low }
must_haves:
  truths:
    - "All blocking source decisions are recorded before fixture generation."
    - "The same workbook hash and approved resolutions produce byte-equivalent canonical output on rerun."
    - "D-03 locations are exactly SR Stock Room plus QC, BL, LU, VC, and SP branches; D-04 opening quantities are retained per location."
    - "Missing codes use deterministic temporary codes per D-07 without turning headings into products."
  artifacts:
    - { path: "prisma/fixtures/opening-catalog.json", provides: "approved canonical products, prices, locations, and opening balances" }
    - { path: "scripts/data-onboarding/generate-seed.ts", provides: "fail-closed deterministic generator" }
  key_links:
    - { from: "resolutions.json", to: "opening-catalog.json", via: "validated finding-ID resolutions" }
    - { from: "source-mapping.json", to: "opening-catalog.json", via: "canonical record source IDs and hashes" }
  prohibitions:
    - statement: "Canonical output must not contain an unresolved source decision or an automatically chosen duplicate, quantity, price, SR source, or BL BEFORE mapping."
      status: unverified
      flagged: true
---

<objective>Turn the owner-approved evidence into a deterministic canonical fixture with complete traceability.</objective>
<execution_context>@C:/Users/Jerum/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Jerum/.config/opencode/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/01-trusted-foundation-and-data-onboarding/01-CONTEXT.md
@.planning/phases/01-trusted-foundation-and-data-onboarding/01-RESEARCH.md
@.planning/phases/01-trusted-foundation-and-data-onboarding/01-PATTERNS.md
@.planning/phases/01-trusted-foundation-and-data-onboarding/01-04-SUMMARY.md
@scripts/data-onboarding/review-report.json
@scripts/data-onboarding/resolutions.json</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Record and validate every owner resolution</name>
  <read_first>01-04-SUMMARY.md owner response; review-report.json; resolutions.json; canonicalize.ts/test; D-03/D-06/D-08/D-09</read_first>
  <files>scripts/data-onboarding/resolutions.json, scripts/data-onboarding/canonicalize.test.ts</files>
  <behavior>- Every blocking finding ID has exactly one schema-valid resolution.
- Unknown, duplicate, stale-workbook-hash, or missing resolution IDs fail generation.
- Owner decisions preserve source evidence rather than overwriting it.</behavior>
  <action>Transcribe the checkpoint response exactly into `resolutions.json`, including reviewer/date, workbook hash, decision reason, and resolved canonical value/disposition. Extend tests to prove complete one-to-one coverage and stale/malformed refusal. Do not reinterpret owner answers.</action>
  <acceptance_criteria>A validation run reports zero unresolved, unknown, or duplicate finding IDs and the workbook hash equals the profile hash.</acceptance_criteria>
  <verify><automated>npm run test -- scripts/data-onboarding/canonicalize.test.ts</automated></verify>
  <done>Every decision required for canonical output exists as reviewed, machine-validated data.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Generate equivalent canonical fixture and trace map</name>
  <reversibility rating="costly">D-01 records and later phases depend on this canonical fixture/mapping contract; changing it requires coordinated schema, seed, and test updates.</reversibility>
  <read_first>D-01-D-07; 01-RESEARCH.md Profile→Review→Generate→Load; 01-PATTERNS.md deterministic keyed-load pattern; validated resolutions.json; source-mapping.json</read_first>
  <files>scripts/data-onboarding/generate-seed.ts, scripts/data-onboarding/generate-seed.test.ts, scripts/data-onboarding/source-mapping.json, prisma/fixtures/opening-catalog.json</files>
  <behavior>- Zero blockers is required before writing output.
- Missing item codes become `TMP-S{sheetIndex}-R{rowNumber}` and any collision fails.
- Output contains six exact locations, approved products/prices, and each per-location opening quantity.
- Two runs with identical inputs produce byte-identical JSON and hashes.</behavior>
  <action>Implement `generateCanonicalFixture` and CLI flags `--profile`, `--resolutions`, `--fixture-out`, `--mapping-out`, and `--check`. Sort records by stable business/source keys, normalize decimal strings without floating-point conversion, embed input/output hashes and source IDs, write atomically only after complete validation, and add byte-equivalence tests. Implement D-01-D-07 exactly.</action>
  <acceptance_criteria>The test generates twice into separate temp paths and compares bytes; `--check` passes for the committed fixture; location set/types and trace IDs match the approved report.</acceptance_criteria>
  <verify><automated>npm run test -- scripts/data-onboarding/generate-seed.test.ts && node scripts/data-onboarding/generate-seed.ts --profile scripts/data-onboarding/review-report.json --resolutions scripts/data-onboarding/resolutions.json --fixture-out prisma/fixtures/opening-catalog.json --mapping-out scripts/data-onboarding/source-mapping.json --check</automated></verify>
  <done>The reviewed fixture is deterministic, complete, canonical, and traceable.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| owner resolutions→generator | Only complete keyed decisions may authorize output. |
| generator→committed fixture | Output becomes authoritative seed input. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-01 | Elevation | cross-branch scope | high | mitigate | 01-07 persisted policy. |
| T-01-02 | Elevation | direct user APIs | high | mitigate | 01-09 Admin guard. |
| T-01-03 | Elevation | retained sessions | high | mitigate | 01-09 transaction. |
| T-01-04 | Tampering | formula/reference poisoning | high | mitigate | Generator consumes reviewed evidence, never evaluates formulas, and verifies hashes. |
| T-01-05 | Tampering/DoS | production reset | critical | mitigate | Fixture generation has no DB writes; 01-06 gates reload. |
| T-01-06 | Elevation | second Admin | high | mitigate | Fixture contains catalog only; 01-06/09 protect Admin. |
| T-01-07 | Elevation | Better Auth overexposure | high | mitigate | No auth endpoints here. |
| T-01-08 | Information Disclosure | temporary password | high | mitigate | Fixture and mapping schemas reject credential fields. |
| T-01-SC | Tampering | dependencies | high | mitigate | Locked approved dependencies only. |
</threat_model>

## Artifacts this phase produces
- Symbols: `generateCanonicalFixture`.
- CLI flags: `--profile`, `--resolutions`, `--fixture-out`, `--mapping-out`, `--check`.
- Fields: `workbookHash`, `resolutionHash`, `fixtureHash`, `sourceIds`, canonical location/product/balance fields.
- Files: `generate-seed.ts/test`, approved `resolutions.json`, updated trace map, `prisma/fixtures/opening-catalog.json`.

<verification>Focused canonicalization/generation suites and committed-fixture `--check` pass.</verification>
<success_criteria>Approved reruns are equivalent and unresolved input cannot leave a partial fixture.</success_criteria>
<output>Create `.planning/phases/01-trusted-foundation-and-data-onboarding/01-05-SUMMARY.md` when done.</output>
